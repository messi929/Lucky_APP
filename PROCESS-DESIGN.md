# 고객 프로세스 설계 — 이상적 플로우

> 2026-07-25. `UPGRADE-PLAN.md`의 결정을 **화면 순서**로 옮긴 문서.
> 판단 축: **획득(전파) + 락인(전환비용)**. 원칙 = `CLAUDE.md`.

---

## 1. 원형 — 실제 철학관의 순서

이 앱은 철학관 상담의 리듬을 재현하는 제품이다. 원형을 먼저 적어두고 우리 플로우를 대조한다.

```
① 들어와 생년월일시를 말한다
② 도사가 명식을 적는다              ← 기다림이 기대를 만든다
③ 도사가 먼저 말한다 "당신은 이런 사람이지"   ← 내가 말 안 했는데 맞혔다
④ 과거를 짚는다 "몇 년도에 이런 일 있었지"     ← 신뢰 확정
⑤ 그제야 손님이 고민을 꺼낸다 "사실 제가…"
⑥ 도사가 그 고민에 답한다
⑦ 마무리 + "또 와"
```

**핵심 순서: 손님이 고민을 말하기 _전에_ 도사가 먼저 맞혀야 한다.**
신뢰가 먼저, 고민은 나중. 이 순서가 뒤집히면 그냥 설문지가 된다.

---

## 2. 현재 플로우와의 간극

```
현재:  랜딩 → 입력 → /r 8카드(일반론) → /s 허브 → /s/{concern} 세션
원형:  ①②   →  ③  →      ★④ 없음     →  ⑤    →      ⑥⑦
```

| 간극 | 증상 |
|---|---|
| **④ 과거 검증이 없다** | 신뢰 방아쇠가 통째로 빠져 있다. `daeun`·`seyun` 재료는 이미 있는데 미사용 |
| **③이 8장으로 늘어져 있다** | 훅 하나면 될 자리에 일반론 8장. 임팩트가 분산되고 ⑤까지 도달 전에 지친다 |
| **"리포트"와 "상담" 두 개념이 경쟁** | 사용자가 `/r`과 `/s` 두 세계를 동시에 이해해야 한다 |
| **⑦ "또 와"가 구조에 없다** | 한 번 다 보면 끝. 재방문 이유가 설계돼 있지 않다 |

---

## 3. 목표 플로우

```
[랜딩]  훅 한 줄 · 입력 전 호기심
   ↓
[입력]  생년월일 8자리 · 시각 4자리 (현행 유지 — 이미 최적화됨)
   ↓
[명식]  붓글씨로 명식이 그려짐 (BrushIntro) · 기대 형성
   ↓
━━━━━━━ /r/{token} — 오프닝 (무료 · 공유 단위) ━━━━━━━
  1. 眞  훅          "당신은 이런 사람이지"        ← ③
  2. 命  명식 + 오행 + 격국 라벨                    ← 전문성 신호 · 스샷 자산
  3. ★   과거 검증   "그 무렵 이러지 않았어요?"     ← ④ 신뢰 방아쇠 [맞아요/아니요]
  4. 運  지금 국면 + 당신의 색·방향                 ← 공유 카드 (전파 자산)
   ↓
━━━━━━━ /s/{token} — 고민 허브 ━━━━━━━
  問  "그래서 오늘, 뭐가 제일 궁금해요?"            ← ⑤
   ↓
━━━━━━━ /s/{token}/{concern} — 집중 세션 ━━━━━━━
  진단(무료)  →  [990원 해금]  →  근거 · 시기 · 처방  ← ⑥
   ↓
  緣  마무리 — 꺾는 문장 + 공유                     ← ⑦ 전파
   ↓
  다음: [이어서 다음 주제] [다른 고민] [다른 사람 사주]  ← ⑦ 재방문
```

### 무료/유료 경계의 원칙

> **일반론은 전부 공짜로 준다. 자기 상황에만 돈을 받는다.**

헤비 유저는 일반론에 돈을 내지 않는다. 일반론은 **전파 재료**로 쓰는 게 훨씬 이득이다.
반대로 "내 결혼 시기"는 일반론이 아니므로 여기서 과금한다.

| | 무료 | 유료(990원/주제) |
|---|---|---|
| 성격·명식·오행·격국 | ✅ 전부 | |
| 과거 검증 | ✅ | |
| **당신의 색·방향** | ✅ (공유 자산) | |
| 고민별 진단 1비트 | ✅ (훅) | |
| **올해의 한 가지**(행동 처방) | | ✅ |
| 근거 · 시기 · 처방 | | ✅ |

---

## 4. 이번에 반영한 것

### 4-1. 처방 카드의 무료/유료 분할 ✅

기존에는 무료 8카드에서 색·방향·**올해의 한 가지**를 다 주고, 그 다음 990원에 세션 처방을 팔았다.
**이미 준 걸 다시 파는 구조.**

그런데 이 카드는 동시에 **공유 자산**이다 — 카카오톡 공유 + 9:16 스토리 이미지 저장이 붙어 있다.
통째로 걷어내면 전파 경로가 사라진다. 그래서 **쪼갰다.**

- 색 · 방향 → **무료 유지** (시각 아이덴티티 · 스샷 가치 · 전파)
- 올해의 한 가지 → **유료** (구체적 행동 처방 = 세션 처방과 직접 경쟁하던 항목)
- 무료일 때 자리는 비우지 않고 `"고민 하나를 고르면, 그 주제로 처방해 드려요"` — 허브로 미는 문구

전파 경로는 지키면서 잠식만 끊는다.

### 4-2. 처방 색·방위 단일 소스화 ✅

`session_remedy` 지시문이 "개운(색 또는 방향) 하나를"이라고만 해서 LLM이 자유 생성했다.
그 결과 같은 화면에서 **"검은색·북쪽"(LLM) vs "초록·동쪽"(코어)** 이 동시에 떴다.

- 프롬프트에 코어 처방값을 **주입**하고 다른 색·방위 생성 금지
- 생성물 **후검증** — 다른 오행의 색·방위가 섞이면 재생성 → 실패 시 안전 폴백
- 한글은 단어 경계가 없어 `"검은색"`의 `"은색"`이 금(metal)으로 오탐되는 문제까지 처리

---

## 5. 과거 검증 카드 ✅ 구현됨

플로우에서 유일하게 비어 있던 칸이자, **임팩트가 가장 큰 칸.** 이번에 구현.

- `packages/core/src/interpret/retro.ts` — `retroProbes(chart)`: 대운 전환 → 질문형 프로브. LLM 0, 결정론.
- `ReportPayload.retro`로 전달 (연도 포함 → LLM 미전달, 원칙 2 유지)
- 웹 `components/RetroCard.tsx` · 앱 `components/RetroCard.tsx` — 타입 카드 직후 삽입
- 응답 [맞아요/아니요]는 기기 로컬 저장 (웹 localStorage / 앱 SecureStore, 원칙 5)
- 십신별 질문 10종 + 폴백. 전량 질문형 · 가드레일 L3 통과 · 건강/사망/법적 어휘 배제
- 테스트 10종 (재현성·질문형·가드레일·나이 적응 포함)

원설계(아래)는 그대로 반영됨:

### 재료 (이미 있음, 미사용)

```
saju.daeun  : [{ startAge:9, startYear:1967, ganzhi:"丁巳", stemTenGod:"정관", … }, …]
saju.seyun  : [{ year:2021, ganzhi:"辛丑", tenGodStem:"겁재", … }, …]
saju.currentAge / currentYear
```

### 설계

1. **대운 전환기**(10년 경계)와 **세운 충돌 연도**를 뽑는다 → 후보 구간 2~3개
2. 그 구간을 **질문형**으로 제시 — 단정 금지
   > "2019년에서 2021년 사이, 자리나 사람이 한 번 크게 바뀌지 않았어요?"
3. **[맞아요 / 아니요] 탭** → 응답 저장
4. "맞아요"면 다음 카드의 어조가 올라가고, "아니요"면 다른 구간으로 한 번 더

### 왜 질문형인가 (획득·락인)

- **하방 차단** — 틀려도 질문이라 손상이 없다. 틀린 단정은 역방향 전파(나쁜 입소문)를 만든다
- **자기생성 근거** — 사용자가 자기 기억으로 채운 검증이 우리가 주장한 것보다 훨씬 끈끈하다
- **캘리브레이션 자산** — 어떤 대운·세운 휴리스틱이 실제로 맞는지 쌓인다.
  경쟁사가 복제할 수 없고, **쓸수록 정확해지는 제품**이 된다
- **개인 히스토리 = 전환비용** — 답변이 쌓인 앱은 버리기 어렵다

### 가드레일 (필수)

- 과거 진술은 **L2(관망 언어) 이상**. "힘들었죠"는 되고 **"아프셨죠"는 안 된다**
- 건강·사망·법적 사건 단정은 L3 차단 대상
- 응답 저장은 기기 로컬 우선 (원칙 5: 회원가입 금지)

---

## 6. 재방문 설계 (⑦ "또 와")

현재 재방문 이유가 구조에 없다. 붙일 수 있는 것들:

| 트리거 | 재료 | 상태 |
|---|---|---|
| 절기 전환 | `astro.ts` 절입 계산 | ✅ **연결됨** (`season.ts` → 허브 스트립) |
| 대운 전환 연도 | `saju.daeun` | 있음 · 미연결 (과거검증엔 사용) |
| 데일리 한 줄(曉) | `daily.ts` | 있음 · 앱 출시 의존 |
| 다른 사람 사주 | 기기 명부 | ✅ 추가됨 |

**절기 전환(A5) 구현**: `saju/season.ts`의 `seasonPhase(now)` — 현재/다음 절기, 경과일,
전환 직후(justTurned)·임박(turningSoon) 판정. 상담 허브(웹·앱)에 스트립으로 노출하되
**전환 ±3일에만** 보인다(상시 노출은 노이즈, 재방문 트리거로만). LLM 0, 결정론.

---

## 7. 열린 질문

1. `/r` 오프닝을 4장으로 줄이면 기존 8카드의 성격·오행·조심할것 카드는 어디로?
   → 성격·오행은 **명식 카드에 통합**, 조심할것은 세션 비트로 흡수하는 안이 유력하나
   세션 비트 구조(SESSION_BEATS 4종) 변경이라 별도 결정 필요.
2. 3,900원 `full_report` SKU와 990원 주제 해금이 **공존 중**. 목표 플로우는 후자 기준이라 전자의 처리 방침 필요.
