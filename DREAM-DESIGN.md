# 꿈 해석(해몽) — 무료 리텐션 훅 설계 (2026-08-18)

> **성격: 무료 전용.** 판매하지 않는다. 목적은 매출이 아니라 **재방문 사유**다.
> 사주는 1회성인데 꿈은 주 단위로 자연 발생한다 — SWOT의 최대 약점(리텐션 훅 얕음)을 메우는 장치.
> 관련: `COMPETITIVE-ANALYSIS-2026-07.md` §5 X4(데일리→단건 퍼널) · `USABILITY-TEST-2026-08-08.md`(무료 구간 가치 부족) · `CLAUDE.md`(불변 원칙).

---

## 0. 설계를 지배하는 제약 하나

**자유 서술을 받지 않는다. 심볼 선택형이다.**

`interpret/cache-key.ts`의 전제는 유닛값이 유한 도메인이라는 것이고, 그래서 "캐시 적중률 90%+ 수렴 → 바이럴 비용 폭탄 방지"가 성립한다. 꿈을 자유 텍스트로 받으면 값이 무한 → 적중률 0 → **조회 1건 = LLM 1콜**.

무료 기능에서 이건 치명적이다. 매출이 0인데 조회는 공유를 타고 늘어난다 — 성공할수록 손해다.

> Haiku 4.5 기준 콜당 입력 ~600 / 출력 ~320 토큰 ≈ **3원**.
> 하루 1만 조회면 3만원/일 ≈ **월 90만원**. 캐시가 없으면 그대로 나간다.

심볼 선택형은 이 문제와 함께 **개인정보 문제도 동시에 없앤다**. 자유 입력이 없으면 이름·직장·가족·건강이 프롬프트에 딸려 들어갈 경로 자체가 사라진다(원칙 2 취지 보존, 경쟁분석이 꼽은 "개인정보 안전" 포지셔닝 유지).

---

## 1. 개인화의 다리 — 심볼에 오행을 부여한다

심볼만 보여주면 검색하면 나오는 해몽 사전과 같다. 우리만 할 수 있는 해석이 되려면 **원국과 엮여야** 한다. 그렇다고 일간(10종)을 캐시 키에 넣으면 조합이 10배가 된다.

해법: 각 심볼에 **오행 1개**를 부여하고, 사용자 원국과의 관계를 **3종으로 축약**해 키에 넣는다.

| 관계 | 판정 | 해석 방향 |
|---|---|---|
| `lack` | 심볼 오행 == `weakestElement` | "네가 비어 있던 자리를 꿈이 건드렸다" — 보완 신호 |
| `excess` | 심볼 오행 == `strongestElement` | "이미 넘치는 기운이 또 나왔다" — 과열 주의 |
| `neutral` | 그 외 | 일반 해석 |

`deriveFacts()`가 이미 `weakestElement` · `strongestElement`를 계산한다. **새 명리 판정을 만들지 않는다** — 기존 값을 재사용만 한다(원칙 3 취지: 보정·판정은 기존 어댑터 안에서).

---

## 2. 캐시 키 설계

```
dream_reading:{symbol}|{mood}|{relation}|{tone}:{promptVersion}:-:{mode}:-
```

`cacheKeyOf()`를 그대로 쓴다. 유닛값만 새로 조립하면 되고 캐시 인프라 변경은 **없다**.

- `symbol` — 60종(1차) → 100종(확장)
- `mood` — 감정 4종 (`fear` 무서움 / `relief` 개운함 / `sad` 슬픔 / `odd` 이상함)
- `relation` — 3종 (위 표)
- `tone` — `deep` / `counter` (기존 반응 축 재사용)
- `mode` — `mz` / `classic` (기존 톤 축)

**총 키 상한 = 60 × 4 × 3 × 2 × 2 = 2,880.** 시즌 축은 쓰지 않는다(`seasonal: false`) — 꿈 해석은 절기를 타지 않고, 시즌을 넣으면 키가 반기마다 통째로 무효화된다.

상위 20% 심볼이 조회의 대부분을 차지할 것이므로 **실질 수렴은 수백 키 규모**다. 수렴 후 한계비용 ≈ 0.

### 왜 심볼 2개 선택을 막는가
2개를 허용하면 `60C2 = 1,770` 조합 × 나머지 축 = **8만 키**. 적중률이 무너진다.
**심볼은 1개만 고르게 한다.** 이게 캐시가 성립하는 유일한 조건이다.

---

## 3. 데이터 — `packages/core/src/content/dreams.ts`

순수 TS. React/Next/RN import 없음, 외부 호출 없음(원칙 1).

```ts
export type DreamMood = "fear" | "relief" | "sad" | "odd";
export type DreamRelation = "lack" | "excess" | "neutral";

export interface DreamSymbol {
  id: DreamSymbolId;
  label: string;              // 선택 UI 표기 ("이빨이 빠졌다")
  aliases: string[];          // 검색 보조 ("치아", "이가 빠지")
  element: Element;           // 원국과 엮는 다리 (§1)
  guardrailLevel: GuardrailLevel;
  classic: string;            // 전통 통설 한 줄 — 정적, LLM 0
  promptTemplate: string;     // LLM 해석 지시
}
```

`classic`이 정적이라는 점이 중요하다. **LLM이 죽어도 화면은 뜬다**(stub 폴백과 같은 방어선). 무료 기능이 유료 경로의 가용성을 갉아먹지 않는다.

### 심볼 1차 60종 (초안)

오행 배정은 전통 상징 대응. 가드레일 3은 민감(임신·죽음·건강), 2는 관망 언어 필요(재물).

| # | id | 표기 | 오행 | GR |
|---|---|---|---|---|
| **동물 (13)** |
|1| `snake` | 뱀 | water | 1 |
|2| `dragon` | 용 | wood | 1 |
|3| `tiger` | 호랑이 | wood | 1 |
|4| `pig` | 돼지 | water | 2 |
|5| `dog` | 개 | earth | 1 |
|6| `cat` | 고양이 | metal | 1 |
|7| `cow` | 소 | earth | 1 |
|8| `horse` | 말 | fire | 1 |
|9| `mouse` | 쥐 | water | 1 |
|10| `bird` | 새 | fire | 1 |
|11| `fish` | 물고기 | water | 1 |
|12| `spider` | 거미 | earth | 1 |
|13| `bug` | 벌레 | earth | 1 |
| **자연 (12)** |
|14| `sea` | 바다 | water | 1 |
|15| `river` | 강·시냇물 | water | 1 |
|16| `flood` | 홍수·물난리 | water | 1 |
|17| `rain` | 비 | water | 1 |
|18| `fire_burning` | 불·화재 | fire | 1 |
|19| `mountain` | 산 | earth | 1 |
|20| `tree` | 나무 | wood | 1 |
|21| `flower` | 꽃 | wood | 1 |
|22| `snow` | 눈 | water | 1 |
|23| `wind` | 바람 | wood | 1 |
|24| `muddy_water` | 흙탕물 | earth | 1 |
|25| `well` | 우물 | water | 1 |
| **몸 (7)** |
|26| `teeth_falling` | 이빨이 빠졌다 | metal | 1 |
|27| `hair` | 머리카락 | wood | 1 |
|28| `blood` | 피 | fire | **3** |
|29| `naked` | 벌거벗었다 | metal | 1 |
|30| `pregnant` | 임신·태몽 | water | **3** |
|31| `feces` | 대변 | earth | 2 |
|32| `wound` | 다쳤다 | metal | **3** |
| **사람 (6)** |
|33| `deceased` | 돌아가신 분 | earth | **3** |
|34| `ex_lover` | 옛 연인 | fire | 1 |
|35| `stranger` | 낯선 사람 | metal | 1 |
|36| `baby` | 아기 | water | **3** |
|37| `celebrity` | 유명인 | fire | 1 |
|38| `family` | 가족 | earth | 1 |
| **상황 (12)** |
|39| `chased` | 쫓겼다 | metal | 1 |
|40| `falling` | 떨어졌다 | earth | 1 |
|41| `flying` | 날았다 | wood | 1 |
|42| `lost` | 길을 잃었다 | earth | 1 |
|43| `exam` | 시험 | wood | 1 |
|44| `late` | 지각·놓쳤다 | fire | 1 |
|45| `fight` | 싸웠다 | fire | 1 |
|46| `death_scene` | 죽는 꿈 | earth | **3** |
|47| `wedding` | 결혼식 | fire | 1 |
|48| `funeral` | 장례식 | earth | **3** |
|49| `moving_house` | 이사 | earth | 1 |
|50| `falling_out` | 헤어졌다 | metal | 1 |
| **사물·장소 (10)** |
|51| `money` | 돈 | metal | **2** |
|52| `gold` | 금·보석 | metal | **2** |
|53| `shoes` | 신발 | earth | 1 |
|54| `mirror` | 거울 | metal | 1 |
|55| `knife` | 칼 | metal | 1 |
|56| `door` | 문 | wood | 1 |
|57| `stairs` | 계단 | earth | 1 |
|58| `car` | 자동차 | metal | 1 |
|59| `toilet` | 화장실 | water | 1 |
|60| `hospital` | 병원 | metal | **3** |

**GR3 = 12종, GR2 = 4종.** 민감 비율이 높다 — 해몽은 흉몽 프레임이 본질이라 예상된 값이고, 그래서 §5의 전용 가드레일이 선택이 아니라 필수다.

---

## 4. 유닛·화면 구조

### UnitKind 추가 (2종)

```ts
| "dream_classic"   // 전통 통설 한 줄 (static, 캐시 키 null)
| "dream_reading"   // 내 원국과 엮은 해석 (llm, 캐시)
```

`dream_classic`은 `resolveStatic()` 분기에 심볼 조회를 한 줄 더하는 정도. `dream_reading`은 기존 `resolveLlm()` 경로를 그대로 탄다 — **파이프라인 수정 없음**.

모델 티어는 `tier: "free"`(Haiku). 무료 기능이 Sonnet을 쓰면 안 된다.

### 화면 (카드 1장 = 뷰포트 1화면, 원칙 7)

| # | 화면 | 내용 | 스탬프 |
|---|---|---|---|
| D1 | 심볼 선택 | 카테고리 탭 + 그리드. **1개만** 선택 | 夢 |
| D2 | 감정 선택 | 4종 큰 버튼 (무서웠다 / 개운했다 / 슬펐다 / 이상했다) | 夢 |
| D3 | 통설 | `classic` 한 줄 + 붓글씨 연출(기존 컴포넌트 재사용) | 夢 |
| D4 | 내 해석 | `dream_reading` — 원국과 엮은 2~3문장 | 夢 |
| D5 | 잇기 | "이 흐름, 올해 전체로 보면?" → 리포트/상담 허브 CTA | 運 |

`classic` 오버라이드 축으로만 분기(원칙 7·디자인 규칙 3). 별도 화면 중복 구현 없음.

> **디자인 규칙 4 갱신 필요**: 낙관 스탬프 한자 목록에 `夢`(꿈) 추가.
> 現 목록: 命 眞 問 答 運 緣 福 吉 曉 → **夢** 추가.

### 라우팅·저장

- 경로 `/dream/{token}` — 토큰이 있어야 원국과 엮을 수 있다. 홈 `ResumeCard`(self 토큰)와 리포트 덱에서 진입.
- **저장하지 않는다.** 무상태. `results`·`interpret_cache` 외 신규 테이블 0.
- 토큰 없는 방문자는 D3(통설)까지만 보고 입력 폼으로 유도 — 이것도 유입 경로가 된다.

---

## 5. 가드레일 — L3 전용 규칙 신설

기존 `fatal`·`fear`·`anxiety`·`spending` 규칙은 그대로 재사용된다. 해몽에서 새로 필요한 것:

```ts
// guardrails.ts RULES에 추가
{ category: "pregnancy", level: 3,
  pattern: /임신(하게|할|한다|이에요|입니다)|태몽(이|이에요|입니다|이니)|아이가\s*(생기|들어)/ },
{ category: "lottery",   level: 2,
  pattern: /복권|로또|당첨|일확천금|한탕/ },
{ category: "omen",      level: 3,
  pattern: /흉몽|불길한\s*꿈|나쁜\s*꿈이니|조상이\s*부르|따라\s*가(게|려)/ },
```

- **`pregnancy`** — 태몽을 임신 단정으로 말하면 의료·개인사 단정이다. "태몽이래요" 한마디가 실제 상황과 어긋나면 그 자체로 상처다.
- **`lottery`** — 돼지·금·돈 심볼에서 가장 흔한 사고. 기존 `investment` 규칙은 주식/코인만 잡는다.
- **`omen`** — "돌아가신 분이 부른다" 류. `fear` 규칙이 못 잡는 해몽 특유의 표현.

프롬프트 쪽 지시(`guardrailDirective`에 해몽 분기 추가):

> ※ 꿈 해석입니다. 길흉을 단정하지 마세요. 꿈은 **지금 마음 상태의 신호**로만 다루고,
> 예언·경고가 아니라 "요즘 이런 게 걸려 있었나 봐요" 프레임으로 쓰세요.
> 임신·죽음·복권은 어떤 경우에도 단정하지 않습니다.

이 프레임("예언이 아니라 마음의 신호")이 **"겁주지 않는 사주" 정체성과 해몽을 화해시키는 지점**이다. 반면교사 목록의 "공포·불안 소구"를 구조적으로 피한다.

---

## 6. 계측 — 도입 목적이 리텐션이므로 KPI가 명확해야 한다

```
dream_open          { source: home|report|push }
dream_symbol_select { symbol }
dream_read          { symbol, mood, relation, cacheHit }
dream_to_report     { }   // D5 CTA 클릭
```

**성공 판정: 꿈 사용자의 7일 내 재방문율 > 비사용자.** 이게 안 나오면 이 기능은 폐기 대상이다. 무료라 매출 지표가 없으므로 재방문율이 유일한 근거다.

`cacheHit` 계측이 특히 중요하다 — 적중률이 수렴하지 않으면 설계 전제가 틀린 것이므로 즉시 중단 판단.

---

## 7. 작업 분해

| | 작업 | 규모 |
|---|---|---|
| D1 | `content/dreams.ts` — 심볼 60종 × (classic 카피 + promptTemplate) | **가장 큼.** 콘텐츠 작성 |
| D2 | `UnitKind` 2종 + `decomposeDreamUnits()` + `unitInstruction` 분기 | 작음 |
| D3 | 가드레일 3규칙 + 프롬프트 지시 | 작음 |
| D4 | `/dream/{token}` 5화면 (세션 덱 컴포넌트 재사용) | 중간 |
| D5 | 진입로 2곳(홈 ResumeCard·리포트 덱) + 계측 | 작음 |
| D6 | 테스트 — 캐시 키 안정성, 가드레일 패턴, 심볼 오행 전수 | 작음 |

**2026-08-18: D1~D6 구현 완료.** 실동작 검증 — 캐시 적중 2.4s→0.10s, 키 형식
`dream_reading:snake|fear|lack|deep:v4:-:mz:-`(설계와 일치), 민감 상징 4종(태몽·죽는꿈·
고인·돈) 모두 재생성·폴백 0회로 안전 생성. 5화면 실브라우저 통과.

코드보다 **D1 콘텐츠가 8할**이다. 60종 × 2필드 = 120개 문장.

---

## 8. 하지 않기로 한 것

- **자유 텍스트 입력** — 캐시·개인정보 두 문제의 근원. §0.
- **심볼 복수 선택** — 조합 폭발. §2.
- **유료화** — 지금은 안 판다. 재방문율이 증명된 뒤에 재론.
- **꿈 기록 저장·히스토리** — 개인 데이터 축적은 우리 포지셔닝과 반대 방향. 무상태 유지.
- **시즌 축** — 절기를 타지 않는데 키만 반기마다 무효화된다.

---

## 9. 선행 조건 (이것부터)

이 기능보다 먼저 끝나야 하는 것 — 결제 경로의 결함이다. 무료 훅을 먼저 붙이면 유입만 늘고 전환은 그대로다.

1. `/s/{token}/health` 원시 에러 노출 (`guardrailLevel` undefined)
2. 990원 경로 청약철회 동의 체크박스 부재 — **원칙 9 위반**
3. 가격 비교 문구가 세션 페이월에만 있음 (990원 결제자 2명 모두 이 문구가 이유였다)
4. 가격·상품명 불일치, 7일 패스 기간·무갱신 고지 부재
