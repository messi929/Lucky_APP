# 세션 핸드오프 — 가상 사용자 8인 사용성 테스트 + 1단계 수정 (2026-08-08)

> 무작위 생성 페르소나 8인이 실브라우저로 전 기능을 사용해 마찰 지점을 수집하고, 그중 코드로
> 해결 가능한 치명적 결함을 수정한 기록. 관련: `USABILITY-TEST-2026-08-08.md`(발견 전문·우선순위) ·
> `SESSION-HANDOFF-2026-07-27.md`(직전) · `CLAUDE.md`(불변원칙).

## 0. 한눈에

- **커밋 없음. 작업트리 미커밋 상태**(GitHub 접근 차단으로 이번 세션은 문서화까지만).
  브랜치 `expo-sdk54-ux`, 시작 시점 `44f71ab` = `origin/main`.
- 변경분: **11파일 수정 + 2파일 신규, +92/−19.**
- 검증: **타입체크 6패키지 · 테스트 110/110 · 웹 빌드 · 실브라우저 재현** 전부 통과.
- 사용성 테스트 산출물: 페르소나 8인, 스크린샷 315장, 발견 **치명적 7 / 큼 19 / 보통 다수**.

### ⚠️ 이번 세션 최대 발견 (코드 밖)

**Supabase 프로젝트가 사라졌다.** `.env.local`의 `chvxifxlgniyfozyfmbd.supabase.co` 가
**ISP DNS·8.8.8.8 양쪽에서 NXDOMAIN**(`supabase.co` 자체는 정상 응답). 무료 플랜은 7일 비활동 시
정지되며 마지막 작업일이 2026-07-27이었다.

이것이 사용자 8명 중 7명의 결제 포기 1위 원인과 직결된다 — `interpret_cache` 조회가 100% 실패해
**조회할 때마다 해석이 새로 생성**되고, 동시에 **페이지 조회 1건마다 LLM이 재호출**된다.
`cache-key.ts` 주석이 명시한 설계 목적("캐시 적중률 90%+ 수렴 → 바이럴 비용 폭탄 방지")이
현재 적중률 0%로 무력화된 상태다.

로컬 재현: 리포트 생성은 `POST /api/report 200`으로 성공하는데 저장이 조용히 실패해, 발급된
`/r/{token}`을 열면 **"결과를 찾지 못했어요. 링크가 만료되었거나 잘못된 주소예요"** 가 뜬다.
저장 실패가 사용자에겐 "만료된 링크"로 보인다.

**프로덕션 영향 미확인** — Vercel 환경변수를 확인하지 못했다. 같은 URL을 쓴다면 프로덕션도 동일 증상.

---

## 1. 테스트 방법

로컬 dev 웹(`:3111`, **실제 LLM 생성 포함**)을 대상으로, 연령·성별·직업·사주이해도·기기숙련도·
성향을 무작위 추첨한 페르소나 8인이 **각자 독립 브라우저 프로필**(persistent context)로 사용.

저장소는 Supabase 소실 때문에 **인메모리로 전환**해 진행(`SUPABASE_URL="" pnpm dev`).
따라서 아래 발견 중 캐시·영속성 관련 항목은 `[저장소 복구 시 해소]` 로 구분해 두었다.

| ID | 페르소나 | 성향 | 기기 | 담당 |
|---|---|---|---|---|
| p1 | 33 남 간호사 | 꼼꼼 | 모바일 | 입력폼·엣지케이스·정책문서 |
| p2 | 44 남 물류관리 | 성급 · 사주 문외한 | 모바일 | 리포트 덱·톤 토글·가독성 |
| p3 | 63 여 영업 | 성급 · 문외한 | 모바일 | 상담 세션·유료벽 |
| p4 | 20 성별미선택 | 성급 | 데스크톱 | 결제 3종·청약철회 |
| p5 | 53 성별미선택 영업 | 의심 · 십신 아는 사람 | 데스크톱 | 궁합 양방향 루프 |
| p6 | 24 성별미선택 교사 | 회의적 · 의심 | 데스크톱 | 공유·제3자 열람 |
| p7 | 47 남 영업 | 산만 | 모바일 | 선물·초대·이탈복귀 |
| p8 | 74 여 | 미숙 · 의심 | 모바일 | 버티컬·오류복구·접근성 |

우연히 야자시 경계가 함께 검증됐다 — p4는 00:03, p7은 23:51 출생. p8은 음력 입력 담당.

**결과 한 줄: 8명 중 3,900원을 낸 사람 0명.** 990원은 2명이 실제 구매했고 둘 다 이유가 같았다 —
**"주제 4개면 3,960원 → 전체 3,900원"** 비교 문구. 이 문구는 세션 페이월에만 있고 메인 결제
화면에는 없다.

> ⚠️ 산출물 소실 주의: 스크린샷 315장과 페르소나별 원본 보고서는 세션 스크래치패드
> (`%LOCALAPPDATA%\Temp\claude\C--src-Lucky-app\9026caf1-.../scratchpad/personas/`)에만 있다.
> 발견 내용은 `USABILITY-TEST-2026-08-08.md` 에 전부 반영했으나, 스크린샷 근거가 필요하면
> 세션 종료 전에 별도 보관할 것.

---

## 2. 이번 세션에서 고친 것 (7건)

| | 변경 | 파일 |
|---|---|---|
| **F1** 로딩 화면 자동 전환 | `BrushIntro`에 **자동 전환 타이머가 아예 없어** 연출 후 무한 정지. 애니메이션 길이(3.06s)에 맞춘 `setTimeout` + `prefers-reduced-motion` 즉시 표시. 부모가 인라인 콜백을 넘기므로 **ref로 고정**(안 그러면 리렌더마다 타이머 되감김) | `components/BrushIntro.tsx` |
| **F2** 문답 게이트 | `isPaid` → `hasFullAccess`. **1,900원 체험패스 구매자가 402로 거절**되던 것 수정. 리포트·세션은 `hasFullAccess`를 쓰는데 문답만 누락돼 있었음 | `app/api/ask/route.ts` |
| **F3** 본인 토큰 저장 | `rememberPerson`이 이미 `self` 플래그를 지원하는데 `/input`에서 **호출만 빠져 있었음**. 저장 추가 | `components/InputForm.tsx` |
| **F4** 홈 이어보기 | 저장만 하면 꺼낼 곳이 없어 `ResumeCard` 신설(클라이언트 조각, 마운트 후 읽어 하이드레이션 불일치 회피, 명부 비면 미렌더) | `components/ResumeCard.tsx`(신규) · `app/page.tsx` |
| **F5** 궁합 토큰 누수 | `/compat`의 결제 링크가 **A의 리포트 토큰을 그대로 노출** → 누구나 `/r/{aToken}`으로 A 리포트 전체 열람 가능했음. 쿼리만 바꾸면 `Checkout`이 클라이언트 컴포넌트라 RSC 페이로드에 재노출되므로, `compat` 참조를 끝까지 내리고 **서버(`/api/checkout`)가 소유자를 역조회**하도록 변경. 결제 후 리다이렉트도 소유자 리포트가 아닌 궁합 결과로 | `app/compat/[compatToken]/page.tsx` · `app/pay/page.tsx` · `components/Checkout.tsx` · `app/api/checkout/route.ts` · `packages/api-client/src/index.ts` |
| **F6** 죽은 버튼 2개 | 리포트 "카카오톡으로 공유"·"앱 푸시"가 `track()`만 호출하고 끝이었음. `lib/share.ts` 신설(네이티브 공유 → 클립보드 → 수동 안내)해 공유 연결. 푸시는 **스토어 링크 자체가 없어**(앱 미출시) 수요 계측만 유지하고 "출시 준비 중" 표기 + 안내 — **임시 처리, 방향 결정 필요** | `components/ReportDeck.tsx` · `lib/share.ts`(신규) |
| **F7** 핀치 줌 차단 해제 | `maximumScale: 1` 제거(WCAG 1.4.4). 74세 페르소나가 11~13px 글자 43덩이를 확대할 수단이 없었음 | `app/layout.tsx` |

### 검증 방법
- 타입체크 6패키지(모바일 포함) · 테스트 110/110 · `pnpm --filter @lucky/web build` 통과.
- F1: 제출 후 **클릭 없이** 10.1화면 리포트 덱 도달 확인.
- F3·F4: `localStorage["palja.people"]`에 `self:true` 기록 + 홈에 "이어보기" 링크 노출 확인.
- F5: 궁합 페이지·`/pay?compat=` 페이지 소스에서 **A 토큰 0회** 확인(이전엔 href에 존재).

### 정정 기록
- **카카오 공유 버튼은 `ReportDeck`의 것만 빈 핸들러였다.** `CompatShare`·`CompatInvite`·`Checkout`의
  공유 버튼은 원래 정상이고 `alert`까지 있다. 테스트 중 "무반응"으로 보인 것은 **헤드리스
  브라우저가 dialog를 자동으로 닫아서** 생긴 아티팩트.
- 반면 p5의 **"궁합 버튼이 스크롤 때문에 클릭이 빗나간다"는 실제로 재현**됨(Playwright 클릭
  15초 타임아웃 → JS 클릭으로 우회). 데스크톱 궁합 진입로에 실제 손실이 있을 수 있음. **미수정.**

---

## 3. 남은 치명적 결함 (미수정)

`USABILITY-TEST-2026-08-08.md` 5장에 5단계 순서로 정리. 핵심만:

### 사용자 판단이 필요해 손대지 않은 것

**유료 해금이 토큰에만 묶여 있다** (`store.ts:35` `hasFullAccess(token)`, `:45` `isSessionUnlocked(token, concern)`).
p6이 990·3,900원 결제 후 **빈 게스트 프로필**로 열었더니 페이월이 아예 안 뜨고 유료 문답까지
정상 작동했다. **공유가 성장축인데 공유가 곧 매출 소멸.**

단순 버그가 아니라 **원칙 5("접근은 토큰(웹)·기기ID(앱)")와 충돌**한다. 기기 결속으로 바꾸면
선물 수신(`/g`)·궁합 역유입이 깨진다. **제안: 공유용 읽기 전용 토큰 별도 발급**(공유 링크는
무료 구간까지만) — 원칙을 지키면서 누수를 막는다. **결정 대기.**

### 사용자 실물 정보/권한이 필요한 것
- **Supabase 복구** (0단계, 위 참조)
- **사업자정보 실값** — `/business` 전 항목 `TODO`인데 결제 버튼 동작 중. 게다가 `/privacy`가
  "삭제 요청은 사업자정보의 이메일로"라고 지목하는데 그 값이 `TODO`라 **이용자 권리 조항이
  구조적으로 무효**. 결제를 실제로 열기 전 필수.

### 코드로 가능하나 이번에 안 한 것 (2단계 이후)
- 무료 구간 가치 부족 — 티저 "조심할 것"이 오행 그래프를 문장으로 옮긴 수준
- 가격·상품명 불일치("전체 리포트+모든 상담" ↔ "풀 리포트+문답 1회"), 세 가격 비교 불가
- 990원 경로에만 청약철회 동의 체크박스 없음(원칙 9)
- 7일 패스의 기간·무갱신 고지가 구매 전후 **0글자**
- 주제 카탈로그 — 티저가 약속한 택일·건강이 허브에 없고, `/s/{token}/health`는
  **`Cannot read properties of undefined (reading 'guardrailLevel')`** 원시 에러 노출
- 사주 용어 설명 부재(상담 세션은 이미 괄호 설명을 하는데 메인 리포트만 안 함)
- 뒤로가기 시 상담 진행 전체 리셋(카드 단계가 히스토리에 없음)
- 생성물 결함 — 문장 중간 잘림, 마크다운 `**` 노출, 오타(`쯔음`·`시기쯯`·`믄고`·`쯐`)

### 명리 감수 필요 (백로그 B2와 같은 뿌리)
- **개운 처방이 "없는 오행 채우기" 한 줄 로직** — p5의 水6 극신강 사주에 金(금생수)을 권해
  역효과. 신강/신약·용신/기신 구분 없음
- **관계 유형 7종이 라벨 치환** — 동일 상대로 부부/연인 둘 다 76점, 문장 완전 동일.
  부모–자녀에 연인용 카피("끌리면서도 부딪히는 힘") 사용
- 십신 용어 오용 3건(월지↔월간, 월주↔월간, 극 방향 반대)

---

## 4. 다음 세션이 받아야 할 것

1. **GitHub 접근 복구 후 커밋** — 현재 미커밋. 기능 단위로 쪼개면 F1 / F2 / F3+F4 / F5 / F6 / F7.
2. **Supabase 상태 확인 결과**에 따라: 일시정지면 Restore, 삭제면 신규 생성 + `supabase/schema.sql`
   실행 + `.env.local`·Vercel 키 갱신.
3. **결정 대기 4건**: 공유 토큰 정책 / 자녀운 990원 vs 9,900원 중복 / 앱 푸시 버튼 방향 /
   요즘말 모드 가드레일 강도(현재 "꼬인 심성"·"관계를 틀어먹어요" 등 단정형이 "겁주지 않는 사주"
   약속과 충돌).
4. **직전 세션 미완 과제 유지**: 앱 실기기 검증 5가지(`SESSION-HANDOFF-2026-07-27.md` §4).
   이번 수정은 전부 웹이므로 앱은 그대로 남아 있음.

### 개발 환경 메모
- dev 서버는 **인메모리 모드**로 띄웠다: `SUPABASE_URL="" SUPABASE_SERVICE_ROLE_KEY="" pnpm dev --port 3111`.
  Supabase 복구 전까지 로컬 테스트는 이 방식이어야 리포트 저장이 동작한다.
- `packages/api-client` 변경 시 web 타입체크가 워크스페이스 참조로 바로 잡힌다(별도 빌드 스크립트 없음).
- 재시작은 **포트 지정 kill만**(`netstat -ano | grep :3111` → 해당 PID) — MCP 서버 보호.
