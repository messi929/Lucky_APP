# 사용성 테스트 보고서 — 가상 사용자 8인 (2026-08-08)

로컬 dev 웹(`:3111`, 실제 LLM 생성 포함)을 대상으로, 연령·성별·성향·기기숙련도를 무작위 추첨한
페르소나 8인이 각자 **독립된 브라우저 프로필**로 전 기능을 사용했다. 총 스크린샷 315장.

> **저장소 주의**: `.env.local`의 Supabase 프로젝트(`chvxifxlgniyfozyfmbd.supabase.co`)가 **DNS 소실**
> 상태(ISP·8.8.8.8 양쪽 NXDOMAIN, `supabase.co` 자체는 정상)여서, 테스트는 인메모리 저장소로 전환해
> 진행했다. 아래 각 항목에 **[저장소 복구 시 해소]** 표기를 달아 구분했다.

## 표본

| ID | 페르소나 | 성향 | 기기 | 담당 |
|---|---|---|---|---|
| p1 | 33 남 간호사 | 꼼꼼 | 모바일 | 입력폼·엣지케이스·정책문서 |
| p2 | 44 남 물류관리 | 성급 · 사주 문외한 | 모바일 | 리포트 덱·톤 토글·가독성 |
| p3 | 63 여 영업 | 성급 · 문외한 | 모바일 | 상담 세션·유료벽 |
| p4 | 20 성별미선택 | 성급 | 데스크톱 | 결제 3종·청약철회 |
| p5 | 53 성별미선택 영업 | 의심 · 십신 아는 사람 | 데스크톱 | 궁합 양방향 루프 |
| p6 | 24 성별미선택 교사 | 회의적 · 의심 | 데스크톱 | 공유·제3자 열람 |
| p7 | 47 남 영업 | 산만 | 모바일 | 선물·초대·이탈복귀 |
| p8 | 74 여 | 미숙 · 의심 | 모바일 | 버티컬·오류복구·접근성 |

## 결과 한 줄

**8명 중 3,900원을 낸 사람은 0명.** 990원은 2명(p2 자녀운, p7 사업·승진)이 실제 구매 의사를 보였고,
둘 다 **"주제 4개면 3,960원 → 전체 3,900원"** 비교 문구 때문에 지갑을 열었다고 답했다.
결제를 막은 이유 1위는 가격이 아니라 **"같은 링크를 다시 열면 내용이 달라진다"** 였다(7명이 독립 발견).

---

# 1. 치명적 — 결제·신뢰를 직접 무너뜨림

## C1. 같은 URL을 다시 열면 해석이 매번 달라진다 (7/8명) [저장소 복구 시 대부분 해소]

단순 변주가 아니라 **자기모순**이다.

- p1: "속은 곱지 않거든요, 남 탓하기 쉽고" ↔ "겉으로는 순하고 배려심 깊어 보이지만"
- p2: "일간이 약하면서도" ↔ "일간이 강한 가운데" (6회 로드 6회 상이)
- p8: "흙이 과하고 금·수가 부족" ↔ "토·금·수가 고르게 받쳐주는 균형잡힌 기질"
- p3: 빠진 오행이 금 → 토 → 토. 본문은 "토가 없다"인데 처방은 금 보완(흰색·서쪽)
- p7: 오행 표 `土 4` vs 본문 "토기둥이 다섯 개"

**원인(확정).** `packages/core/src/interpret/cache-key.ts`의 키는
`유닛:값:프롬프트버전:시즌:톤모드:관심사` 로 **난수·타임스탬프가 없는 완전 결정적** 값이다.
즉 설계는 "같은 사주 → 같은 글"이 맞고, 캐시만 적중하면 안정적이다.
문제는 `apps/web/lib/cache.ts` 가 Supabase 부재 시 **프로세스 내 `Map`** 으로 폴백한다는 점이다.

**프로덕션 영향.** 현재 프로덕션은 `getSupabase()`가 URL·키 존재로 클라이언트를 만들지만 도메인이
없어 모든 쿼리가 실패 → `get`이 항상 null → **모든 조회마다 LLM 재호출**. 이는 신뢰 문제이자
비용 문제다. `cache-key.ts` 주석이 설계 목적을 명시한다 — *"캐시 적중률 90%+ 수렴 → 바이럴 비용
폭탄 방지"*. 지금은 적중률 0%다.

**저장소를 복구해도 남는 것.** 캐시는 *유닛 값* 기준이지 *토큰* 기준이 아니다. 프롬프트 버전을
올리면(설계상 의도된 "자연 무효화") **이미 공유된 리포트의 문장이 소급 변경**되고, 받은 사람은
보낸 사람이 본 것과 다른 글을 본다. 공유가 성장축인 서비스에서는 별도 판단이 필요하다.

## C2. 리포트가 완성됐는데 로딩 화면이 자동 전환되지 않는다 (5/8명)

`/input` 제출 → `/r/{token}` "당신의 팔자를 적고 있어요". p2 측정 기준 **21초에 이미 완성**된 뒤
**111초까지 화면이 1픽셀도 변하지 않는다**. p8은 `waitFor` 60초 타임아웃까지 동일했다.
그런데 하단의 작고 테두리만 있는 **[바로 보기]** 를 누르면 10화면 리포트가 **즉시** 뜬다.

- p2: "실제 사용자였으면 여기서 앱을 껐습니다"
- p8(74세): "적고 있어요"는 "아직 만드는 중", "바로 보기"는 "대충 미리보기"로 읽혀 **절대 안 누른다**

**다 만들어놓고 사용자를 놓치는 중이다.** 대기 시간 문제가 아니라 완료 사실을 화면이 안 알리는 문제.

## C3. 유료 해금이 기기가 아니라 토큰에 붙어 있다 — 링크만 있으면 공짜 (코드 확인)

`apps/web/lib/store.ts:35` `hasFullAccess(token)`, `:45` `isSessionUnlocked(token, concern)` —
어디에도 기기ID·구매자 식별자가 없다.

p6이 990원·3,900원 결제 후 **localStorage가 완전히 빈 게스트 프로필**로 `/r/{token}`과
`/s/{token}/relationship`을 열자 페이월이 아예 뜨지 않았고, 유료 카드 전부는 물론 **"팔자에게 묻기"
문답까지 정상 동작**해 게스트가 실제로 질문을 제출하고 AI 답변을 받았다.

**공유를 성장축으로 삼는 서비스에서 공유가 곧 매출 소멸이 되는 구조다.**

## C4. 리포트를 다시 찾을 방법이 없다 — 결제물이 URL 하나에 매달려 있다 (3명)

p7이 리포트 4개 생성 + 3,900원·990원 결제 후 확인: `localStorage=[]`, 쿠키 없음,
`sessionStorage=0`, 홈에 "이어보기" 없음. **`/r/{token}` URL을 잃으면 결제한 콘텐츠가 영구 소실.**

p7은 같은 브라우저에서 **동일 생일을 재입력**했더니 새 토큰이 발급되고 이미 990원 낸 주제에
**다시 990원을 청구**했다. 중복 결제·환불 민원 직결.

p8은 생성 대기 중 뒤로가기를 눌렀더니 **about:blank** 로 나가버렸고 복구 경로가 없었다.
p5도 생성 중 창을 닫아 리포트를 통째로 날리고 처음부터 다시 했다.

**인프라는 이미 있다.** `apps/web/lib/people.ts` 에 localStorage 기반 "봐준 사람" 명부가 있으나
`PersonForm`(다른 사람 보기)에서만 쓰이고, **`/input`으로 만든 본인 토큰은 저장하지 않는다.**
호출 한 곳만 추가하면 되는 문제다.

## C5. 궁합 결과가 초대자의 리포트 토큰을 노출한다 (p5·p7)

`/compat/...`의 "관계 상세 보기 · 2,900원" 링크가 `/pay?token={A의_리포트_토큰}&sku=compat_detail`
형태로 HTML에 그대로 박혀 있다. `?me=` 없이 열어도 동일.

p5가 **다른 브라우저 프로필에서 `/r/{그 토큰}`을 열자 아무 확인 절차 없이 A의 리포트 전체가
열렸다.** "자랑하기"로 단톡방에 뿌리면 그 방 전원이 A의 열쇠를 갖는다.

p5의 지적이 정확하다 — *"생년월일은 노출 안 된다는 반론은 안 통한다. 사주 여덟 글자가 곧
생년월일시다."* 게다가 화면에 "경도 126.5°E"(→지역), "그때 46살 무렵"(→출생년)이 함께 찍힌다.
첫 화면에서 "이름/전화번호 안 물어봄"을 자랑하는 서비스가 궁합 한 번에 원국을 통째로 흘린다.

## C6. 1,900원 "전체 열람" 패스 구매자가 문답에서 402로 거절된다 (코드 확인)

`apps/web/app/api/ask/route.ts:19` 가 `isPaid(body.token)` — **영구 결제만 통과시키는 함수**를 쓴다.
바로 옆에 "영구 결제 OR 유효 체험 패스"를 뜻하는 `hasFullAccess()`가 이미 있는데 문답만 안 쓴다.

리포트·세션 게이트는 `hasFullAccess`를 쓰므로 열리는데 문답만 안 열려, 사용자에겐 "돈 냈는데
막혔다"가 된다. 게다가 입력창이 **활성 상태**라 질문을 다 타이핑한 뒤에야 거절당하고, 거절
메시지에 가격도 링크도 없어 막다른 길이다. p4가 "사기당했다"고 느낀 유일한 지점.

## C7. 사업자정보 전 항목이 `TODO`인데 결제 버튼이 살아 있다 (5명)

`/business` 의 상호·대표자·주소·사업자등록번호·통신판매업 신고번호·이메일이 전부 `TODO`.
그런데 `/privacy` 5항은 "삭제 요청은 사업자정보의 이메일로"라고 지목 → **이용자 권리 조항이
구조적으로 무효**. `/terms` 3조는 "열람 전이라면 철회 가능"이라 하지만 **신청 창구·연락처·취소
버튼이 어디에도 없다**. 비회원 결제라 결제자를 특정할 방법도 없다.

의심 성향 페르소나 3명(p5·p6·p8) 전원이 **여기서 결제 의사가 0이 됐다**고 답했다.

---

# 2. 큼 — 전환·이해를 막음

## G1. 죽은 버튼 2개 — 계측만 붙고 기능이 없다 (코드 확인)

- `ReportDeck.tsx:308` **"카카오톡으로 공유"** → `track("share_click", …)` 만 호출하고 끝.
  공유도, 클립보드 복사도, 창 열기도 없음. p5·p6·p7이 "눌러도 반응이 없어 여러 번 눌렀다".
  p6 계측: `window.Kakao`=undefined, `window.open` 미호출, `navigator.share` 미호출, 콘솔 에러도 없음.
- `ReportDeck.tsx:332` **"매일 아침, 오늘의 한 줄 / 앱에서 푸시로 받아보기"** → `track("app_install_click")` 만.

**대시보드에는 클릭이 쌓이므로 지표상 정상으로 보였을 가능성이 크다.** 성장축(공유)과 리텐션
축(푸시)의 종착점이 둘 다 비어 있다.

## G2. 핀치 줌이 앱 전역에서 차단돼 있다 (코드 확인)

`apps/web/app/layout.tsx:32` `maximumScale: 1`. WCAG 1.4.4 위반.
p8(74세)이 리포트 한 페이지에서 11px×8, 12px×24, 13px×11 = **43덩이**의 작은 글자를 만났는데
확대할 유일한 수단이 막혀 있다. 한 줄 삭제로 해결된다.

특히 **조작 요소가 작다**: 톤 토글 12px·57×30px(버튼처럼 안 보임),
"다른 고민 보기 — 자녀·택일…" **13px·높이 20px**(버티컬로 가는 유일한 입구인데 캡션으로 보임),
"먼저 7일만 둘러볼래요 · 1,900원" **13px·높이 20px**(결제 링크가 20픽셀).

## G3. 사주 용어 설명이 리포트 어디에도 없다 (문외한 4명 전원)

p2가 10화면 전수 조사: "일주", "본체", "오행의 균형", "진태양시 보정", "월간 甲木 상관이 일간
癸水를 생하면서", "겁재가 연·일·시에 세 곳" — **툴팁·용어집·각주가 단 한 군데도 없다.**

p3이 못 알아들은 말 목록: 일주, 일지, 월간, 시주, 편인, 편관, 겁재, 비견, 재성, 원국, 종왕격,
용신, 대운, 세운, 갑인일, 신유.

**그런데 상담 세션에서는 이미 괄호 설명을 한다** — "일지 亥의 **겁재(형제·경쟁)**",
"용신 **甲(성장의 틀)**". 만들 줄 아는 기법을 메인 리포트에서 안 쓰고 있다.

유일하게 통한 것은 **"나의 타입 — 癸 이슬 / 감수성·직관·온화·소진"**, **"戊 = 큰 산"** 카드였다.
p2: *"10화면 중 유일하게 설명 없이 바로 와닿았습니다."*

## G4. 무료 구간의 가치가 0에 가까워 유료 전환이 막힌다

무료 티저 "조심할 것"이 **두 화면 위 막대그래프를 문장으로 옮긴 것뿐**이다 —
"금 오행이 완전히 비어 있습니다"(p2), "수가 완전히 비어있으니까 감정 소통에서 쉽게 막혀요".

p2: *"공짜 샘플이 이 수준이면 나머지 셋도 이렇겠구나 싶었습니다."*
p3의 세션 무료 구간은 진단 **딱 한 문장** + 초심자가 못 읽는 한자 근거 1문단이 전부였고, 정작
답인 연도("2027~2028년쯤")는 유료 뒤에 있었다. → *"결국 아무 답도 안 줬다"*.
무료 분량도 주제마다 1~6문장으로 들쭉날쭉하다.

**성급한 사용자에게는 길어서 지치는 것보다 짧아서 성의 없어 보이는 게 더 치명적이다.**

## G5. 청약철회 동의 절차가 경로마다 다르다 — 가장 많이 팔릴 상품이 가장 약하다

- `/pay`(1,900·3,900): 별도 동의 컨트롤 + 미체크 시 "청약철회 제한 안내에 동의해 주세요."로
  **정확히 차단되고 사전 체크도 안 돼 있다(양호)**.
- **세션 내 990원**: 체크박스 없이 **[동의하고 열기 · 990원]** 버튼 하나에 동의를 병합.

CLAUDE.md 원칙 9("명시적 동의 체크 단계가 반드시 포함") 대비 990원 경로가 취약하다.
부수로 `/pay`의 동의 컨트롤은 `<input type=checkbox>`가 아니라 **텍스트·aria-label이 없는 빈
`<button aria-pressed>`** 이고 옆 문구를 눌러도 체크되지 않는다. p7은 이걸 선물 토글과 혼동해
실제로 오조작했다.

## G6. 주제 카탈로그가 화면과 어긋난다 + 알 수 없는 주제는 500으로 깨진다

- 리포트 티저는 "다른 고민 보기 — 자녀·**택일**·**건강**한 한 해 …"인데 **허브에 택일도 건강도 없다**(p3·p6·p8).
- `/s/{token}/health` → 화면에 날것의 JS 에러 **"Cannot read properties of undefined (reading 'guardrailLevel')"** 노출(직접 재현).
- p3의 핵심 고민 `contract_timing`(계약·퇴사)은 **60대 가중치 0.1이라 허브 상위 6개에서 탈락**하지만,
  URL로 직접 열면 정상 동작하고 **이번 테스트 통틀어 품질이 가장 좋았다**. 있는 걸 못 팔고 있다.
- p7의 데일리 CTA가 추천한 `job`은 **허브 목록에 없는 유령 주제**다.
- "전체 주제 보기" 탈출구가 없어 사용자는 노출된 6개가 전부인 줄 안다.

p3(63세, 현역 영업): *"이 서비스는 나를 은퇴해서 손주 보는 할머니로 정해놨다."*

## G7. 뒤로가기 한 번에 상담 진행이 전부 리셋된다 (p3·p7·p8)

상담 카드 전환이 히스토리에 쌓이지 않아 `history.back()`이 **카드 1장이 아니라 세션 전체를
이탈**시킨다. 복귀하면 리포트 스플래시(scrollY=0)로 튕기고 **먹글씨 애니메이션이 다시 재생되며
"바로 보기"를 또 눌러야 한다**. 세션 안에 [이전] 버튼이 아예 없고, 허브·세션·세션종료 어디에도
**"내 리포트로" 링크가 없다**. 진행 표시는 지름 6px 점뿐.

## G8. 타인의 생년월일을 대신 입력하는 데 동의 절차가 없다 (p5)

p5가 초대 링크를 직접 열어 아내·어머니 생일을 넣었고, 아무것도 묻지 않은 채 **어머니의 전체
리포트가 생성**됐다. 어머니는 서비스 존재도 모른다.
`/privacy` 6항에 "동의를 받은 정보만 입력해야 합니다" 한 줄이 있지만 **정작 입력 화면엔 한 글자도
없다** — 책임만 사용자에게 떠넘기는 구조로 읽힌다. 결제 청약철회는 체크박스까지 만들어 놓고
남의 개인정보엔 없다.

부수: 초대 링크가 **만료도 1회용도 아니다**. 이미 사용된 `/c/{토큰}`을 타 프로필에서 다시 열면
입력 폼이 그대로 재노출되고, 관계 유형을 누를 때마다 **새 토큰이 계속 발급**되어 노출면이 누적된다.

## G9. 궁합 결과가 초대자에게 돌아오지 않는다 — 루프가 실질 한 방향 (p5·p7)

궁합 3건을 끝내도 A의 리포트 `緣` 섹션에 이력이 전혀 안 남는다(HTML에 `compat` 문자열 자체가 없음).
실제 상황에서 상대가 자기 폰으로 입력하면 **초대자는 결과를 영영 못 본다**.
그래서 사용자가 "그냥 내가 대신 입력하지"로 우회하고, 그게 G8을 키운다.

## G10. 관계 유형 7종이 라벨 치환에 불과하다 (p5)

동일 상대로 **부부/연인 → 둘 다 76점, 문장 완전 동일**, 관계 단어만 교체.
부모–자녀(65점)는 점수는 다른데 **카피가 연인용 그대로**다 — "싸우면서 정드는",
"끌리면서도 부딪히는 힘". 어머니 궁합에 "끌린다"는 부적절하다.

명리로도 성립하지 않는다. 부부는 일지·관성/재성, 친구·직장은 비겁·식상을 보므로 보는 자리가
다른데 점수가 같을 수 없다.

## G11. 톤 토글이 어미만 바꾸고 난이도는 그대로 (p2)

- 정중히: "일간 癸水가 강하면서 겹친 겁재·비견이 시주와 연주를 점하고 있으니…"
- 요즘말: "겁재가 연월일시에 깔려 있죠. 자기 것을 지키려는 집념이 강한데…"

**겁재·상관·일간은 양쪽 다 그대로.** 문외한에겐 둘 다 똑같이 어렵다. 누르기 전에 무슨 기능인지
알 수 없고, 재생성 20초 동안 피드백이 하단의 작은 "다시 헤아리는 중…" 알약뿐이라 두세 번 누르게
된다. 그 상단 토글이 스크롤 중 **본문과 버튼 글자를 계속 가린다**(p2: `[글쎄요, 잘 모르겠는데]`
라벨이 잘려 읽히지 않음).

## G12. 가격·상품명이 화면마다 다르고, 세 가격이 한 화면에 공존하지 않는다

- 세션 페이월 **"전체 리포트 + 모든 상담 · 3,900원"** ↔ `/pay` **"풀 리포트 + 문답 1회 · 3,900원"**
- 리포트 페이월=3,900/1,900, 세션 페이월=990/3,900, `/pay`=URL sku가 정한 1개만(다른 선택지 링크조차 없음)
  → **990 vs 1,900 비교가 구조적으로 불가능**
- "주제 4개면 3,960원" 앵커는 실제 주제가 6개라 **자기 값을 낮게 부르고 있다**
- 이미 낸 990원이 전체 구매에서 차감되는지 **아무 설명이 없다**
- 같은 "자녀운"이 **990원**(`/s/…/child_fortune`)과 **9,900원**(`/vertical/child`) 두 가격, 차이 설명 없음

## G13. 7일 패스의 기간·갱신 설명이 구매 전후 통틀어 0글자 (p4)

`/pay?sku=season_pass` 전체 문구는 "7일 체험 패스 · 전체 열람 / 1,900원"이 전부.
**"자동 갱신 없음", "7일 뒤 잠깁니다", 만료일이 한 글자도 없다.** 구매 후 리포트에도 D-day·만료
표시 없음(`7일|만료|남았|D-|기간` 매치 0건).

무갱신 기간제라는 사실이 화면에 없으니 20대·의심 성향에겐 기본값이 "자동결제"로 읽혀 **가장 안
사고 싶은 상품**이 됐다. p4: *"이 한 줄만 있으면 990원 대신 이걸 샀을 것."*

## G14. 생성물 품질 결함 — 문장 잘림·마크다운 노출·오타

- **문장이 중간에서 끝남**: "…정리하는 게"(p3), "…통보하듯 밀고 나가는"·"…나무 기운을 가까"(p6),
  "…느껴오셨을"·"…패턴인데,"(p8)
- **마크다운 원문 노출**: "하반기에 `**감정 표현이 과해지거나 …**` 시기"(p2·p5)
- **오타·깨짐**: "2025~2026년**쯔음**", "시기**쯯**", "**믄고** 지켜보는"(p2), "2027~2028년**쯐**",
  "**재재 재단**하는"(p3), "당신 주(**十**)의"(p5)
- **조사 처리**: "부부**으**로 잘 맞는 편이에요"(p5)

## G15. 해석의 명리 논리 오류 (p5 — 십신 아는 사용자)

- **개운 처방이 역행한다.** p5 원국은 水6·木1·土1·火0·金0 극신강인데 처방이 **金**(흰색·은색·서쪽).
  금생수라 물을 더 늘린다. 같은 화면 본문은 "물이 넘쳐서 문제"라 써놓고 바로 아래에서 물을 늘리는
  색을 권한다. 로직이 **"없는 오행을 채운다"** 한 줄로 보이며 신강/신약·용신/기신 구분이 없다.
- **십신 용어 오용**: "月支의 癸水 겁재"(→癸水는 월간) / "월간이 계축"(→계축은 월주) /
  B리포트 "월간 편재가 일간을 극하는"(→금극목이라 방향이 정반대)

p5: *"용어를 아는 사람에게 이게 보이면 안 보이는 부분도 못 믿는다."*

## G16. 공유받은 사람에게 신규 유입 진입로가 0개 (p6)

게스트 페이지의 링크 전수 조사 결과 **전부 공유자 토큰**(`/s/{내토큰}/...`, `/pay?token={내토큰}`)뿐.
`/input`·홈 링크 없음("paljareport.com"은 텍스트일 뿐). 게다가 남이 열어도 **"당신의 팔자"** 라
불러서 자기 사주로 오해하고, 그 상태로 "복채 내기"를 누르면 **남의 토큰에 결제**한다.
(아이러니하게 404 페이지에는 "내 사주 보러 가기" 버튼이 있다.)

## G17. 공유 자산이 비어 있어 올릴 이유가 없다 (p6·p7)

`/api/og/story/{token}`(1080×1920)·`opengraph-image`(1200×630) 모두 **한글 정상, 생년월일·성별·지역
미노출(잘함)**. 그러나 내용이 "이슬 + 일반 문구 + 내 사주 카드 리포트·30초"가 전부, 절반이 빈 여백,
**URL/QR 없음**. og:title·description도 완전 일반 문구라 단톡방에서 광고와 구분되지 않는다.
"이미지 저장"은 `<a target="_blank">`에 `download` 속성이 없어 데스크톱에선 새 탭에 PNG만 뜬다.

## G18. 자녀 상담이 아이 정보를 한 번도 묻지 않는다 (p2·p8)

근거는 전부 부모 사주인데 "당신 자녀는 남다른 능력과 욕구가 강한데", "이 아이는 '내가 충분한가'를
끊임없이 재는 심리 회로가 만들어져 있거든요"라고 아이를 단정한다.
p2: *"알아채고 나니 앞서 좋았던 문장도 '아무한테나 하는 말이었나' 싶어졌습니다. 차라리 아이
생년월일도 물었으면 훨씬 믿었고 돈도 더 냈을 겁니다."*

## G19. "겁주지 않는 사주" 약속 위반 — 요즘말 모드 (p6)

수집된 실제 문장: "원래 답답하고 **꼬인 심성**이 있는데", "**관계를 틀어먹어요**", "따뜻함과
적극성이 결핍된 기질이 **당신의 평생 과제**거든요", "**미련한 고집**으로 튈 수 있거든요",
"자기 약함을 **극도로 질타하는**". 전부 조건·확률 없는 단정형이다.

p6: *"'겁주지 않는'을 지키는 건 정중히 모드뿐."*

---

# 3. 보통 — 이해·완성도

- **입력 폼 검증**(p1): 4자리만 입력 시 에러 없이 회색 버튼만 죽어 있어 왜 못 누르는지 모름 /
  시·분 오류를 한 번에 하나씩만 알려줌 / 에러 문구가 회색이라 형식 예시를 밀어냄 /
  **음력 토글이 아무것도 안 바꾸고 윤달 선택지가 없음** / **지역 미선택 시 말없이 서울(127.5°E)로
  계산하면서 "정밀 만세력"이라고 표기**
- **지역 칩이 가로 스크롤인데 힌트가 없다**(p1): 화면엔 서울~대전 6개만 보여 **강원이 10번째라
  존재를 모름**. 랜딩의 "출생지 경도까지 반영" 카피와 충돌
- **입력 개인정보 약속과 처리방침 불일치**(p1): `/input` "생일은 **사주 계산에만**" ↔ `/privacy`
  "데일리 운세 발송, **서비스 개선 통계**". 방침에 있는 "닉네임 수집"이 폼에 없고, 수탁자
  Vercel·Supabase·Anthropic이 전부 해외인데 **국외이전 안내 없음**, **개인정보 보호책임자 없음**,
  보유기간이 "필요한 기간"뿐. 약관·방침 첫 줄이 "초안 — 런칭 전 법무 검토 예정"인데 그 문서로
  청약철회 동의를 받고 있음
- **"시간 몰라요" 리포트가 존재하지 않는 시주를 오행에 포함**(p1): 3주만 표시하면서 오행 합계가
  **8자**로 시각 입력본과 동일. "나머지 세 기둥으로 봅니다" 안내가 사실과 다르고, 시각이 없는데도
  "진태양시 보정" 문구가 붙음 *(엔진 확인 필요 — 미검증)*
- **결제 후 본문이 빈칸이 됨**(p6): 3,900원 결제 후 "성격의 코어"가 비어버림(무료일 땐 있었음, 2회 재현)
- **광고한 "월별 캘린더"가 결제 후에도 없음**(p6)
- **결제 영수증·주문내역·완료 화면 없음**(p6·p7): "결제 중…" 후 리포트로 튕김. 금액·주문번호 전무
- **선물 결제 화면에서 선물이 조용히 취소됨**(p7): 선물 토글을 누르면 메시지 입력값이 사라지고
  버튼이 `선물 결제` → `복채 내기`로 무경고 전환. 페이지 제목도 "복채"라 선물 화면인지 불명확
- **개봉된 선물 링크 재방문 시 처음부터 생일 재입력 요구**(p7) — 받는 사람 복구 경로 없음,
  링크 유출 시 제3자 개봉 가능
- **오류 화면이 상황마다 문구·도착지가 다름**(p8): `/r/*` → "만료되었거나"+[내 사주 보러 가기],
  `/s/*` → "찾을 수 없어요"+[처음으로]→`/input`, `/asdfasdf` → "팔자에 없는 길"+[처음으로]→`/`.
  74세에겐 "만료"가 **"내가 낸 990원도 날아갔구나 + 내가 뭘 잘못 눌렀나"** 로 읽힘
- **훅 반응 버튼을 눌러도 바뀐 곳이 화면 밖**(p2): "소름 돋았어요"를 눌러도 바뀌는 건 한참 아래
  "성격의 코어". 게다가 C1 때문에 새로고침만 해도 바뀌므로 **내가 누른 결과인지 구분 불가**
- **훅 질문이 두루뭉술**(p2): "2020~2022년 사이 자리나 직함이 바뀌지 않았어요?" — 40대 직장인이면
  3년 안에 안 바뀐 사람이 드물어 오히려 의심이 커짐
- **상담 근거가 주제마다 같은 말의 재활용**(p3): 세 주제 모두 "편인이라서, 목이 많아서, 뭐가 없어서"
- **"편인" 뜻풀이가 화면마다 다름**(p3): "스승·제약·집착" / "창의·실행 에너지" / "지키는 것보다 쓰는 것"
- **유료 상담 내부 모순**(p7): 2장 "버티면 선택지가 좁아진다, 외부 기회를 보라" ↔ 4장 "지금 움직이면
  남 좋은 일만, 다지는 쪽에". 3장은 "2025~2026이 좋은 구간인데 기다리라"인데 오늘은 2026년 8월
- **990원이 주는 2장 중 처방은 무료 개운 처방전의 재탕**(p4): 주제가 달라도 동일 → 실질 신규 1장
- **데스크톱 방치**(p4·p6): 1440px에서 본문은 가운데 500px, `/pay`는 상품 카드와 결제 버튼 사이
  약 600px 공백 → 로딩 실패로 오인. 궁합 결과 페이지도 중앙에 거대한 빈 공백
- **입력값 되짚기 없음**(p7): 리포트 어디에도 "1979년 7월 10일 23:51 · 대전"이 없어 한자 8글자만으로는
  검증 불가. "진태양시 보정"이라 자랑하면서 보정 결과 시각을 안 보여줌 — 야자시 경계 출생자에겐 치명적
- **초대에 발신자 표시가 없음**(p5): "壬 '바다' 타입인 분이 부부 궁합을 요청했어요" — 받는 사람이
  누가 보냈는지 몰라 **피싱으로 보임**
- **React CSS shorthand 경고 반복**: `borderColor`와 `border` 혼용(콘솔, 무해하나 스타일 버그 소지)

---

# 4. 실제로 효과가 있었던 것 — 반드시 지킬 것

- **만세력 계산 엔진이 정확하다.** p5(십신 아는 사용자)가 교차 검증: 1/16 입춘 전이라 전년
  간지(壬子), 시두법(壬일 寅시=壬寅), **서울 09:30을 LMT −32분 보정해 巳시가 아니라 辰시로 잡은
  것**까지 정확. p8의 음력 1952.9.10 → 壬辰/庚戌/丁未/乙巳 변환도 정확.
  p5: *"엔진이 이렇게 정확한데 해석 레이어가 다 깎아먹고 있다."*
- **궁합 계산 근거도 맞다**(p5): 庚→壬 금생수 "상생", 己土가 壬水를 극해 "긴장감 있는 조합",
  일지 子–丑 **자축 육합**을 정확히 짚음.
- **"복채"라는 말**(p3, 63세): *"이건 내가 아는 말이다. '결제/구독'보다 저항이 훨씬 적었다."*
- **"담연 도사가 헤아리는 중…"**(p3): *"63살한테 'AI 분석 중'보다 훨씬 낫다."*
- **990원 앵커링 "주제 4개면 3,960원 — 전체 3,900원"** — 실제로 지갑을 연 유일한 문구(p2·p3·p7).
  **메인 결제 화면에는 이 비교가 없다.**
- **유료벽이 무엇을 잠갔는지 이름을 댄다**(p3): "시기·처방 두 장". *"요즘 앱들은 그냥 흐리게 가리고
  돈 내라고만 하는데 이건 정직해 보였다."*
- **`/pay` 청약철회 동의 게이트가 실제로 작동**(p2·p4·p6·p7·p8): 미체크 시 차단, 기본 체크 아님.
- **연령 적응이 실제로 작동**(p8): 74세에게 노후재정·자식손주 노출, 기본 톤이 '정중히'.
- **상담 4장 구조(진단→근거→시기→처방)**(p6): *"2번 '근거' 카드에서 8글자를 다시 띄우고 짚어주는
  흐름은 회의론자에게도 읽을 맛이 있었다."*
- **큰 [다음] 버튼 기반 상담 카드**(p3·p8): 74세가 **유일하게 편히 쓸 수 있는 화면**. 스와이프를
  못 하는 사용자도 전부 조작 가능.
- **한자를 우리말로 바꾼 타입 카드**("癸 이슬", "戊 큰 산") — 문외한에게 통한 유일한 설명 장치.
- **[맞아요] 즉시 되받아치기**(p2): 바로 밑에 "그쵸. 사주는 이렇게 지나온 자리부터 맞혀요." +
  [하나 더 짚어볼까요]. *"이 서비스에서 제일 잘 만든 부분."*
- **나이 계산이 정확한 과거 짚기**(p7): "2019~2021년 사이, 혼자 있고 싶어서 사람을 줄이지
  않았어요? 그때 41살 무렵이에요."
- **공유 이미지에 개인정보가 없다**(p6): 육안 + `2002|0823|전남|16:50` 정규식 검사 0건.
- **선물 수신 화면이 원칙을 지킨다**(p7): `/g/{token}` "앱 설치 불필요 · 복채는 이미 냈답니다"
  + 보낸 메시지 노출 + 생일 한 번으로 해제. **가입·설치 요구 전무.**
- **초대 화면이 초대자 생년월일을 안 보여준다**(p5): "壬 '바다' 타입인 분"까지만 노출.
  p5: *"입구는 잘 막아놓고 출구(궁합 결과)에서 새는 구조라 더 아쉽다."*
- **성별 "선택 안 함"이 끝까지 지켜진다**(p4): 리포트·상담 전체에서 성별 지칭 문구가 한 번도 없음.
- **404 문구 "여긴 팔자에 없는 길이네요"**(p8).
- **정중히 모드가 "겁주지 않는" 약속을 지킨다**(p6): "…해 보이십니다", "조심스럽게 짚어드리고
  싶습니다" + 더 긴 면책.

---

# 5. 권장 수정 순서

## 0단계 — 인프라 (다른 모든 것의 전제)
1. **Supabase 프로젝트 상태 확인·복구.** 이게 안 되면 C1이 계속 재발하고, 조회마다 LLM이
   재호출되어 **공유가 퍼질수록 비용이 선형 증가**한다.

## 1단계 — 결제를 막는 것 (모두 소규모 수정)
2. **C2 로딩 자동 전환** — 완성 즉시 전환. 버튼 문구를 [바로 보기] → **[다 됐어요, 리포트 열기]**,
   검정 채움으로. 5명이 이탈 지점으로 지목했고 수정 난이도는 가장 낮다.
3. **C6 문답 게이트** — `api/ask/route.ts:19` `isPaid` → `hasFullAccess`. **한 줄.**
4. **C3 유료 해금을 구매자에 묶기** — 토큰 단독이 아니라 기기ID/구매 세션 병행. 공유 방문자에겐
   무료 구간까지만 + "나도 내 사주 보기".
5. **C4 본인 토큰 저장** — 이미 있는 `lib/people.ts` 를 `/input` 성공 시에도 호출하고 홈에
   "이어보기" 카드. 동일 입력 재제출 시 기존 토큰 복구 제안.
6. **C5 궁합 결제 링크에서 초대자 토큰 제거** — 서버에서 `compat_id`로 소유자 조회.
7. **C7 사업자정보 실값 + 환불 문의처** — 결제 버튼을 살리기 전에.
8. **G1 죽은 버튼 2개** — 카카오 공유(폴백: 클립보드 복사 + 토스트), 앱 푸시(최소 "출시 알림" 상태).

## 2단계 — 전환율
9. **G4 무료 구간에 "답의 한 조각"** — 시기 카드의 **연도 범위 한 줄만 무료 공개**하고 이유·처방을
   유료로. 주제별 무료 분량 균일화(2~3문장).
10. **G12·G13 가격 정리** — 상품명 통일, 결제 화면에 받는 항목 목록, **990원 앵커 문구를 메인
    페이월에도 이식**, 7일 패스에 "자동 갱신 없음 · MM/DD 만료" 명시.
11. **G5 990원 경로에도 동일한 동의 체크박스** (원칙 9).
12. **G6 주제 카탈로그** — 허브에 "여기 없는 고민 찾기 →" 전체 목록, 티저가 약속한 택일·건강을
    실제로 넣거나 문구에서 빼기, 알 수 없는 슬러그 500 → 안내 화면.

## 3단계 — 이해·접근성
13. **G2 `maximumScale: 1` 삭제** + 최소 16px/터치 48px + 20px짜리 결제·버티컬 링크를 버튼으로 승격.
14. **G3 용어 괄호 설명** — 상담 세션에서 이미 쓰는 방식을 메인 리포트에 이식, 첫 등장 1회만.
    core에 용어 사전 단일 소스를 두어 "편인"이 화면마다 다르게 풀리는 것 방지.
15. **G7 뒤로가기·동선** — 상담 카드 단계를 URL에(`?card=3`), 재방문 시 스플래시 스킵,
    모든 `/s/`·`/pay` 상단에 "← 내 리포트" 고정.
16. **G14 생성물 검증** — 문장 잘림 리트라이, 마크다운 이스케이프, 조사 처리(이미 절기에서 해결한 방식).

## 4단계 — 콘텐츠·로직 (명리 감수 필요)
17. **G15 개운 처방을 신강/신약 → 용신 로직으로.** 현재 "없는 오행 채우기"는 아는 사람에게 즉시 티난다.
18. **G10 관계 유형 실제 분화** — 관계별 가중치와 카피 분리(부모–자녀에 "끌린다" 제거).
19. **G19 요즘말 모드 가드레일** — "꼬인 심성/틀어먹어요/미련한 고집/평생 과제" 등 규정·비난 표현
    금칙, 단정형 → 관망형. 정중히 모드의 강도를 이식.
20. **G18 자녀 상담에 아이 생년월일 선택 입력.**
21. **G8 타인 정보 입력 동의 체크** + 초대 링크 만료·1회성.

## 5단계 — 성장
22. **G16 공유 수신자 전용 UI** — "친구가 보낸 리포트예요" 배지, "당신의 팔자" → "이 사람의 팔자",
    하단 고정 "나도 30초 만에 내 팔자 보기".
23. **G17 공유 자산 채우기** — 오행 막대 + 일주 + 훅 한 문장 + 도메인/QR, og:title 개인화.
    "이미지 저장"에 `download` 속성.
24. **G9 궁합 결과를 초대자에게 회수** — A의 리포트에 완료 목록 누적.

---

## 부록 — 산출물 위치

```
scratchpad/personas/{p1..p8}/shots/    스크린샷 315장
scratchpad/personas/p1/report.md       (p1·p4는 파일 저장됨)
scratchpad/personas/p4/report.md
scratchpad/pw.mjs                      브라우저 하니스 (페르소나별 독립 프로필)
scratchpad/personas.json               추첨된 페르소나 정의
```

p2·p3·p5·p6·p7·p8은 에이전트 샌드박스의 파일 쓰기 제한으로 개별 파일이 저장되지 않았으며,
내용은 본 종합 보고서에 반영되어 있다.
