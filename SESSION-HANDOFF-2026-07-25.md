# 세션 핸드오프 — 상담 프로세스 고도화 (2026-07-25)

> 한 세션에서 상담 경험·해석 품질을 고도화하고 프로덕션까지 배포한 기록.
> 판단 축 = **획득(전파) + 락인(전환비용)**. 관련: `UPGRADE-PLAN.md`(계획·결정) ·
> `PROCESS-DESIGN.md`(플로우 설계) · `CLAUDE.md`(원칙).

## 0. 한눈에

- 커밋 **6건** (`af6bbbf` → `7396d12`). **main·expo-sdk54-ux 동기화 · 프로덕션 배포·검증 완료.**
- 웹은 프로덕션 라이브(`web-eight-olive-98.vercel.app`), 유료 해금 E2E까지 실환경 검증됨.
- **앱(모바일) 변경분은 typecheck만 통과 · 실기기 미확인** (git push는 웹만 배포).
- 검증: typecheck 7/7 · **109 tests**(70→109) · web build · 프로덕션 E2E.

```
7396d12 chore: concern_unlocks 프로덕션 마이그레이션 파일
0b79985 feat: 절기 전환 재방문 훅 (A5)
a8e9c21 feat: 생성 품질 계측 (B6)
ff61db2 feat: 세션 비트 간 일관성 (B4)
2d617bf feat: 과거 검증 카드 (A1)
d8ff4ef feat: 상담 프로세스 고도화 (재입력·타인사주·처방정합 B1/B3)
```

---

## 1. 무엇을 왜 바꿨나

렌즈: **사주 헤비 유저는 "결정 승인·자기 서사·희망 앵커·정상성 확인"을 사러 온다.**
신뢰는 **과거를 맞혔을 때** 생기고(미래는 검증 불가), 전파는 **소름·비교** 순간에 일어난다.

### d8ff4ef — 프로세스 고도화 (B1·B3 + UX)

| 항목 | 문제 → 해결 |
|---|---|
| **재입력 버그** 🐛 | 세션→택일 이동 시 `?token=` 누락 → 생년월일 재입력 강요. `SessionView.tsx:236` 수정 (택일로 넘어가는 concern 5종에서 밟힘) |
| **마무리 카드 선택지** | 유료 사용자가 오히려 선택지 적던 문제 → 이어서/다른고민/다른사람 3버튼 (웹·앱) |
| **타인 사주 = 단독 리포트** | `/p/new`(웹)·`app/person/new.tsx`(앱). 독립 토큰 + 기기 로컬 명부. 앱은 `?token=`으로 대상 전환(기존엔 기기 토큰 1개만 봐서 타인이 내 상담으로 샜음) |
| **처방 오행 동점 버그**(B1) | 최소값 동점 시 목→화→토→금→수 앞선 게 무조건 이김 → 동점일 때만 `advanced.yongsin`으로 가름. 실측 `{목0화1토5금2수0}`: wood(초록·동쪽) → **water(검정·북쪽)** |
| **처방 색·방위 단일소스**(B3) | LLM 자유생성으로 코어와 어긋남(검은색·북쪽 vs 초록·동쪽) → 프롬프트에 코어값 주입 + 후검증(`remedyConflicts`) → 재생성 → 폴백. 한글 단어경계 오탐("검은색"의 "은색"=금)도 처리 |
| **처방 카드 무료/유료 분할** | 색·방향=무료 유지(카톡·9:16 공유 자산이라 통째 이관 불가), '올해의 한 가지'만 유료(세션 처방과 경쟁하던 항목) |

### 2d617bf — 과거 검증 카드 (A1) ★임팩트 최대

- **미사용이던 `saju.daeun`(대운) 자산 활용.** `retro.ts`의 `retroProbes(chart)`: 대운 전환 → 질문형 프로브. LLM 0.
- **질문형만** ("…있었죠" 단정 금지, "…않았어요?"). 틀려도 손상 없음(틀린 단정=역방향 전파).
- **[맞아요/아니요] 응답을 기기 로컬 저장** → 자기생성 근거(가장 끈끈) + 캘리브레이션 자산(쓸수록 정확) + 전환비용.
- 타입 카드 직후 삽입(웹·앱). 십신별 질문 10종, 전량 L3 통과·건강/사망/법적 어휘 배제.

### ff61db2 — 세션 비트 간 일관성 (B4)

- 4비트가 순차 생성되나 앞 비트 텍스트를 못 봄 → 진단이 앵커인데 후속이 그걸 안 봄.
- `buildPrompt`에 `prior` 주입. 진단→근거→시기→처방이 서로 이어지게.
- **캐시 키엔 prior 안 넣음** — 진단이 캐시 키로 안정적이라 하위 비트도 자연 정합. 키 폭발 없음.
- `PROMPT_VERSION` v2→v3 (B3+B4 프롬프트 변경 반영, 캐시 무효화).

### a8e9c21 — 생성 품질 계측 (B6)

- **측정 없이는 개선 판단 불가.** `collectMetrics(units)`: 캐시적중률·폴백율·재생성율·반려사유(guardrail/remedy).
- core 순수 유지(record 호출 없음) — 메타만 반환, 기록은 웹 라우트.
- `GET /api/metrics` 집계 조회. events 버퍼는 **globalThis** (Next dev 라우트별 격리 번들이라 안 그러면 계측 0).
- ⚠️ 인메모리 집계는 단일 인스턴스·재시작 초기화. 장기 집계는 Supabase `events` SQL로(후속).

### 0b79985 — 절기 전환 재방문 훅 (A5)

- 재방문 이유가 구조에 없었음. `season.ts`의 `seasonPhase(now)`: 현재/다음 절기·경과일·전환 판정. LLM 0.
- 상담 허브(웹·앱)에 스트립. **전환 ±3일에만 노출**(상시=노이즈). 스탬프 추가 없음(問 유지), 금박 악센트(주홍 아님).
- 버그 수정: 평시 문구 "지금는"→"지금은"(조사는 '지금'에 붙음).

---

## 2. 배포 · 인프라

### 프로덕션 배포 (라이브)
- `main` push → 자동 프로덕션 배포 **정상 작동**(빌드 1~2분, 바로 안 뜬다고 실패 오판 말 것).
- 현재 프로덕션 = `7396d12`. 게이트 활성(`GET / → 307`), 초대 코드 10개.
- 확인법: Vercel API로 배포 소스 조회(`beta-launch-ops` 메모리 참조). `vercel ls`는 전파 지연.

### concern_unlocks 마이그레이션 (해결됨)
- 프로덕션 Supabase에 테이블이 없어 유료 해금이 조용히 "미해제" 폴백되던 상태(선재 TODO).
- **2026-07-25 SQL 에디터로 수동 적용 완료** (`supabase/migrations/concern_unlocks.sql`).
- DDL은 서비스 롤 키로 REST 실행 불가 → 항상 SQL 에디터 필요.
- **프로덕션 E2E 검증**: 리포트→해금(INSERT)→재조회 4비트(READ)→주제 격리(job 잠김)→DB 레코드 확인. 테스트 코드·레코드 회수·삭제 완료.

---

## 3. ⚠️ 다음 세션 주의사항

1. **앱 변경분 실기기 미확인** — RetroCard·타인사주(`person/new`)·절기 스트립 전부 typecheck만 통과.
   Expo(`exp://<LAN>:8081`)로 상담→마무리→다른사람→과거검증까지 실기기 확인 필요.
2. **결제는 아직 프리뷰** — `/api/session/unlock`은 동의만 받고 바로 해금. 실 IAP/토스 전환 시
   unlock 호출을 **결제 confirm 뒤로** 옮기고 원칙 5/9 개정 필요. **3,900원 full_report vs 990원 주제해금 공존** 정리 필요(사업 결정).
3. **core 수정 시 로컬 dev는 `pnpm --filter @lucky/core build` 필요**(stale dist 함정). 프로덕션은 매 배포 재빌드라 무관.
4. **개발 서버 재시작은 포트 지정 kill만** — `taskkill //IM node.exe`는 MCP 서버까지 죽임.
   `netstat -ano | grep ":3111" | grep LISTENING` → 그 PID만.
5. **꺾는 문장 18종 아직 draft** — 마무리=공유 단위라 공유·재방문 지표로 실검증 후 확정(B6 계측 활용).

---

## 4. 남은 백로그 (우선순위)

- [ ] **앱 실기기 UX 점검** (이번 변경분 + 작은 폰 넘침·붓글씨 타이밍)
- [ ] **가독성·타이포 개선** (사용자 지적 2026-07-25, 상세 §5) — 폰트 로딩·본문 크기·대비
- [ ] **결제 정책 결정** → IAP 전환 여부 (3,900 vs 990 공존 정리) — 사업 판단
- [ ] **A4 마무리 카드 공유** — 꺾는 문장 이미지화(전파). OG 한글 폰트 임베딩 선행
- [ ] B2 격국·용신 전면 채택 — **명리 감수 필요**(현재는 동점 해소에만 제한적 사용)
- [ ] 프로덕션 계측 장기 집계 — Supabase `events` SQL 대시보드
- [ ] Phase 8 폴리시: 통신판매업 신고·사업자정보(런칭 게이트)

---

## 5. 가독성·타이포 개선 (사용자 지적 "가독성이 좀 떨어져")

현재 타이포 실측 후 정리한 구체 문제. 디자인 원칙(`design/`, HTML `:root` 단일 소스)과
`packages/ui/src/tokens.ts`가 색·서체 단일 소스이므로 여기부터 손댈 것.

| # | 문제 | 근거 | 개선 방향 |
|---|---|---|---|
| 1 | **웹 폰트 미서브셋 로딩** ★가장 체감 큼 | `layout.tsx`가 Google Fonts `<link>` + `display=swap`. 한글 Noto 풀셋은 수 MB, 서브셋 안 됨 → 로딩 중 `system-ui` 폴백이 보이고(FOUT) 폰트 교체 시 리플로우 | `next/font/google`로 전환(자동 서브셋·self-host·preload·CLS 방지) 또는 한글 서브셋 self-host. 앱은 expo-google-fonts로 이미 로드됨(웹만 문제) |
| 2 | **본문·캡션이 한글엔 작음** | `.sub` 13px, `.fine` 11px, 타일 부제 11.5px (`globals.css`·모바일 `tSub`). 한글 글자는 라틴보다 조밀 | 본문 최소 14px, 캡션 12px로 상향. `--base-font` 16px는 유지 |
| 3 | **낮은 대비 — WCAG AA 미달** | `--ink-40`(#8d877d) on `--paper`(#F5F0E8) ≈ 대비 2점대(AA 4.5:1 미달). `.fine`·`.sub`·타일부제·eyebrow가 이 색 | 본문성 텍스트는 `--ink-70`(#4a443c) 이상. `--ink-40`은 순수 장식 라벨(eyebrow 등)에만 한정 |
| 4 | **serif 900(Black) 헤드라인 무게** | `--serif` 헤드라인이 Noto Serif KR 900. 긴 문장·작은 사이즈에서 뭉개짐 | 큰 훅은 900 유지, 20px 이하 serif는 700로. 꺾는 문장 등 긴 serif 검토 |
| 5 | **eyebrow 극단 자간** | `.eyebrow` letter-spacing 0.5em (`globals.css:67`) | 라벨 장식으로는 의도적 — 유지. 단 본문·부제엔 절대 금지(이미 지켜짐, 회귀 주의) |

**작업 시 주의**: 색·서체 변경은 `design/design-spec-v1.2.html`의 `:root`가 기준(원칙). HTML 값과
`packages/ui` 토큰 불일치 시 HTML 기준으로 이식. 웹·앱 양쪽 반영(단일 소스, 원칙 8).
접근성 목표: WCAG 2.1 AA(본문 4.5:1, 큰 텍스트 3:1).
