# 세션 핸드오프 — 경쟁분석 기반 성장·수익화 개선 (2026-07-27)

> 경쟁 앱 13종 전수 분석으로 개선 로드맵을 세우고, 획득(바이럴)·전환(유료벽·번들)·리텐션(퍼널·페르소나)
> 축을 웹·앱에 구현·배포한 기록. 관련: `COMPETITIVE-ANALYSIS-2026-07.md`(분석·로드맵) ·
> `SESSION-HANDOFF-2026-07-25.md`(직전) · `CLAUDE.md`(불변원칙) · 메모리 `competitive-analysis-2026-07`.

## 0. 한눈에

- 커밋 **14건** (`38e7715` → `8a91238`), **42파일 +726/−169**. **main·expo-sdk54-ux 동기화 · 프로덕션 배포·검증 완료.**
- 프로덕션 라이브(`web-eight-olive-98.vercel.app`), 현재 origin/main = **`8a91238`**.
- 검증: **테스트 110개**(109→110) · 웹 build · 모바일 typecheck · **웹 시각 QA(실렌더 스크린샷)**.
- **앱(모바일) 변경분은 typecheck + 코드리뷰만 · 실기기 미확인** (다음 세션 과제).

```
8a91238 가독성 #4 — 20px↓ serif 700 + 개인정보 카피 대비
6f6c22e content: 꺾는 문장 18종 확정(final) + 화자 이름 담연 확정
543b4f1 fix: 앱 리뷰 버그 3건
fb0ca36 가독성 #2·#3 — 본문·캡션 상향 + 대비 AA
3047d63 X3 저가 체험 패스 (7일 1,900원)
913d72a X7 공유 OG 이미지 (한글 폰트 임베딩)
fabb803 X5 상담 화자 페르소나 (담연)
90111a3 X4 데일리→단건 퍼널
3def571 X1 궁합 양방향 바이럴 루프
b538f85 N2 결제 번들 앵커링
b7567d7 N4 정확도 배지 + N5 개인정보 카피
bbb1e8f N1 상담 유료벽 이동
a9c77ba N3 웹 폰트 self-host
38e7715 docs: 경쟁 앱 13종 전수 분석
```

---

## 1. 경쟁 분석 (근거)

국내외 13종(포스텔러·점신·정통사주·천을귀인·헬로우봇·결심사·디어리·연애의과학·Co-Star·The Pattern·Nebula·Sanctuary·CHANI·Stellium)을 6축으로 전수 조사 → `COMPETITIVE-ANALYSIS-2026-07.md` + 시각 브리핑 아티팩트.

**결론 3줄**
1. **구조적 공백 3중**(어느 경쟁사도 동시 미보유): ① 겁주지 않는 위로 톤 ② 상담형 세션 UX(업계가 리포트→상담 이동 중, 우리는 이미 4비트) ③ 개인정보 안전(생일 원본 미전달, 원칙2).
2. **성장축=바이럴 유일**(자본으로 점신·포스텔러 못 이김). "공유 수령자 설치 불필요(/r)"는 비어있는 포지션 — 유일 저마찰 사례 연애의과학은 프라이버시로 자멸. 궁합 양방향 역유입은 국내 부재 → 선점.
3. **수익화: 단건 매절 유지**(Stellium 근거). LTV는 구독이 아니라 번들·시즌권·데일리 퍼널로.

---

## 2. 무엇을 왜 바꿨나

### NOW — 저비용·고임팩트

| | 변경 | 근거 |
|---|---|---|
| **N1** 유료벽 이동 | 무료 경계를 진단만 → **진단·근거**로. 시기·처방(실행 답)만 유료. core `units.ts:177` 무료셋에 `session_reason` 추가, 웹·앱 세션 카드 재구성, 회귀테스트 갱신 | 챗봇3사 "절정 직전 결제" 정석 |
| **N2** 번들 앵커링 | `session_unlock`(990) SKU 신설. 세션 결제 카드에 "주제 4개=3,960 → 전체 3,900" 앵커 + 990/3,900 병렬 버튼. **full_report는 isPaid→isSessionUnlocked로 이미 전 주제 해금**(스토리지 계약 기존) | The Pattern·Sanctuary 번들 |
| **N3** 웹 폰트 self-host | `layout.tsx` Google `<link>` → `next/font/google`(Noto Serif/Sans KR). FOUT·리플로우 제거, self-host | 가독성 백로그 #1 |
| **N4** 정확도 신뢰 배지 | `chart.meta` 실제 보정값(경도·서머타임·진태양시)을 payload로 흘려 원국 카드에 "정밀 만세력 · 경도 127.0°E 진태양시 보정". **원본 날짜 미전달**(원칙2) | 천을귀인 정밀 소구 |
| **N5** 개인정보 안심 카피 | 입력폼에 "원본은 AI 해석에 넘기지 않고 간지·오행 값만"(웹·앱) | CHANI·연애의과학 반사이익 |

### NEXT — 성장·리텐션

| | 변경 | 근거 |
|---|---|---|
| **X1** 궁합 양방향 루프 | 궁합 solve가 **B 결과 토큰 발급** → 결과 "내 사주 전체 보기"가 재입력 없이 `/r/{bToken}`(역유입) → B 리포트 하단 CompatInvite로 루프 종결. `me`는 URL로만(공유엔 미포함, 누수 방지). "자랑하기" 재공유(REL-3) 복원. **스키마 변경 0**. 웹·앱 | 궁합 단방향 공백 |
| **X4** 데일리→단건 퍼널 | 오늘의 십신→어울리는 주제 매핑(`dailyConcernFor`, 연령 가용 우선·폴백). 리포트 데일리 카드 밑 "오늘의 결이 궁금하면 · [주제] 상담 →" CTA로 990 세션 | CHANI·일본 LINE 퍼널 |
| **X5** 화자 페르소나 | core content `CONSULTANT`(이름·호칭·태그라인) 단일소스. 상담 허브·세션 로딩에 "담연 도사 · 겁주지 않고 결을 짚어드려요". 생성 코어 불변(캐시 무효화 없음). 웹·앱 | 헬로우봇 라마마·결심사 캐리 |
| **X7** 공유 OG 이미지 | Google Fonts 서브셋(text=) 런타임 fetch로 **Noto Serif KR를 satori에 임베딩** → 한글 두부(□) 해결(폴백 무회귀). `/r` + **`/c`(초대)·`/compat`(궁합)** OG 신설 → X1 루프 카톡 썸네일 확산. **렌더 검증 완료** | Co-Star 공유 카피 |
| **X3** 저가 체험 패스 | `season_pass` SKU(**1,900원·7일**, 튜닝 가능 기본값). 만료는 **`orders.created_at` 윈도우로 판정 → DDL 불필요**. `fulfillOrder`는 패스에 markPaid 안 함(기간제). `hasFullAccess`=영구 OR 유효패스 → 리포트·세션 게이트. 페이월에 "먼저 7일만" 다리(990↔3,900) | 일본 정액·인트로 미끼 |

### 품질 — 가독성·버그·콘텐츠

- **가독성**(백로그 #1~#5 사실상 종결): #2 `.sub`14·`.fine`12px · #3 대비 AA(`.fine`·타일부제 ink-40→ink-70) · #4 ≤20px serif 텍스트 900→700(단일 한자 마크·stub은 유지). eyebrow는 장식이라 ink-40 유지.
- **앱 코드리뷰 버그 3건**(`543b4f1`): ① 절기 `seasonHook` turningSoon 으로/로 조사 받침 구분("입춘로"→"입춘으로", core·회귀테스트) ② 모바일 ReportDeck 세션 진입에 `?token=` 전파(타인 리포트에서 기기 토큰으로 새던 것) ③ 처방전 "이미지 저장" 무반응 버튼 배선.
- **콘텐츠 확정**(`6f6c22e`, 사용자 승인): 꺾는 문장 **18종 전량 draft→final** · 화자 이름 **담연 확정**.

---

## 3. 검증

- **테스트 110/110**(season 조사 회귀 +1), **웹 build**, **모바일 typecheck** 각 커밋마다 green.
- **웹 시각 QA**(실렌더 스크린샷, 모바일 뷰포트): 상담허브(X5·가독성)·세션 페이월(N1·N2)·데일리 퍼널(X4)·N4 배지·리포트 페이월(X3)·입력폼(N5) **전 화면 시각 버그 0**. 콘솔 에러는 favicon 404 하나(무해).
- **OG 렌더 검증**: `/r`·`/c`·`/compat` 1200×630 한글 정상(두부 없음) PNG 확인.

---

## 4. ⚠️ 다음 세션 주의사항 / 남은 작업

1. **앱 실기기 검증(최우선)** — 이번 변경 다수가 앱 typecheck+코드리뷰만. Expo로 리뷰의 "우선 확인 5가지" 확인:
   ① 궁합 역유입(수신자) ② 타인 리포트에서 세션 진입 ③ 절기 임박 문구 ④ 처방전 저장 버튼 ⑤ 구독 화면 레이아웃.
   *(코드 버그는 선제 수정됨. #1 역유입 베타게이트는 웹 SSR·앱 자격으로 실제 무해 — 게이트 해제 시 완전 무관.)*
2. **결제는 여전히 프리뷰** — 세션 990은 즉시 해금(결제 없음), 1,900·3,900은 토스 Checkout(베타에서 미완결 가능). **가격 정책 확정 + 실 IAP/토스 전환 + unlock을 confirm 뒤로 + 원칙 5/9 개정**은 사용자 지시로 **프로세스 완성 후 보류**.
3. **core 수정 시 `pnpm --filter @lucky/core build`** 필요(stale dist). 프로덕션은 매 배포 재빌드라 무관.
4. **개발 서버 재시작은 포트 지정 kill만** — `netstat | grep :3111 LISTENING` → 그 PID만. MCP 서버 보호.
5. **DDL은 SQL 에디터 수동** — 서비스 롤 키로 REST 실행 불가. (X3는 DDL 회피 설계라 무관.)

### 결정 기록 (기본값 — 확정/조정 필요)
- **화자 이름 "담연"** — 확정(2026-07-27). 변경 시 `consultant.ts` 한 곳.
- **체험패스 1,900원·7일** — 기본값. `SKUS.season_pass` + `SEASON_PASS_DAYS`(사업 확정 시 조정).
- **가격 사다리** 990(주제)/1,900(7일)/3,900(영구) — 실결제 전환 때 재검토.

### 남은 백로그
- 엔진: B2 격국·용신 전면 채택(명리 감수 필요) · 야자시 등 경계 재확인.
- 관측: N1 전환율(`/api/metrics` B6) · 생성 품질 장기 집계(Supabase events).
- LATER 로드맵(분석 문서): L1 리포트 상위티어 · L2 공유 심리테스트 · L3 신년팩 · L4 사람 상담 · L5 홈피드 3층.
- Phase 8 폴리시: 통신판매업·사업자정보(결제 연동 게이트).
