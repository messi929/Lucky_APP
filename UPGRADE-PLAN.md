# 고도화 계획 — 상담 경험 · 해석 품질

> 2026-07-25 작성. 범위 = **상담 경험 자체** + **해석 품질·정합성** 두 트랙.
> 렌즈: *사주를 자주 보는 사람*의 입장 → 호기심을 자극하고, 남에게 소개하고 싶어지는 앱.
> 관련: `CONSULT-FLOW-HANDOFF.md`(직전 작업) · `BETA-HANDOFF.md`(배포 상태) · `CLAUDE.md`(원칙)

---

## 0. 렌즈 — 헤비 유저는 무엇을 사러 오는가

사주를 반복 소비하는 사람은 "미래 정보"를 사는 게 아니다. 네 가지를 사러 온다.

| JTBD | 속마음 | 지금 우리 제품의 대응 |
|---|---|---|
| **결정 승인** | "이 선택 해도 되는 거지?" | 세션 시기·처방 비트 — 대응 있음 |
| **자기 서사** | "내가 왜 이런 사람인지 설명해줘" | 일주 훅·일간 타입 — 대응 있음 |
| **희망 앵커** | "언제쯤 나아져?" | 시기 비트 — 대응 있음 |
| **정상성 확인** | "나만 이런 거 아니지?" | ❌ **없음** |

그리고 이들이 **신뢰**를 결정하는 순간은 딱 하나다 — **과거를 맞혔을 때.**
미래 예측은 검증이 불가능하니 신뢰의 근거가 못 된다. "작년 그맘때 힘들지 않았어요?"가 맞으면
그 다음 말은 전부 무게가 실린다. 철학관이 수천 년 써온 기법이고, **우리는 이걸 안 쓰고 있다.**

**전파(소개하고 싶음)** 는 신뢰의 부산물이 아니라 별도 설계 대상이다. 사람이 사주 얘기를 옮길 때는
① 소름 돋았을 때 ② 웃겼을 때 ③ 남과 비교하고 싶을 때. 셋 다 지금 설계에 장치가 없다.

---

## 1. 진단 — 코드로 확인한 것

이 계획의 근거는 추측이 아니라 오늘 실제로 확인한 값이다.

### 1-1. 이미 계산해 놓고 안 쓰는 자산이 많다 🔴

`computeSaju()`가 반환하는 `saju.advanced` / `saju.daeun` / `saju.seyun`에 이런 게 들어 있다:

```
advanced.geukguk : "종왕격"
advanced.yongsin : ["己","丙","癸"]
advanced.dayStrength : { strength: "strong", score: 102 }
advanced.interpretation : "일간이 매우 강한 종왕격입니다. 기운의 방출과 절제의 균형이 핵심입니다…"
daeun : [{ startAge:9, startYear:1967, ganzhi:"丁巳", stemTenGod:"정관", … }, …]  // 10년 단위 전 생애
seyun : [{ year:2021, ganzhi:"辛丑", tenGodStem:"겁재", … }, …]                    // 연도별
currentAge / currentYear
```

**해석 레이어는 이 중 `dayStrength`만 쓴다.** 격국·용신·대운·세운은 전부 버려진다.
→ 신뢰의 방아쇠(과거 적중)를 만들 재료가 이미 있는데 안 쓰는 상태.

### 1-2. 처방 로직이 틀렸다 🔴 (오늘 실제 모순 발견)

유료 세션 처방 카드에서 **LLM은 "검은색·북쪽", 결정론적 카드는 "초록·청록·동쪽"** 이 동시에 떴다.
원인을 파보니 LLM 탓이 아니라 **코어 쪽 결함**이었다.

```ts
// packages/core/src/interpret/units.ts:48-64
let weakest: Element = "wood";
let min = Infinity;
for (const [ko, el] of order) {   // 순서: 목 화 토 금 수
  const v = fe[ko] ?? 0;
  if (v < min) { min = v; weakest = el; }   // ← 동점이면 먼저 온 원소가 이긴다
}
```

실제 케이스(1958-07-02): `{목:0, 화:1, 토:5, 금:2, 수:0}`
→ 목0에서 min=0 확정, 수0은 `0 < 0` = false → **weakest = 목(wood)** 으로 고정.
→ 처방 = 초록·청록·동쪽. 그런데 **수도 똑같이 0이다.** 동점 처리가 없어서 앞선 원소가 무조건 이긴다.

문제는 두 겹이다.

1. **동점 버그** — 0이 여러 개인 원국은 흔하다. 지금은 항상 목→화→토→금→수 순으로 앞선 게 뽑힌다.
2. **더 근본적으로, "가장 적은 오행을 보충한다"는 규칙 자체가 이 사주엔 틀렸다.**
   ssaju는 이 명식을 **종왕격**(강한 기운을 거스르지 않고 따라가는 격)으로 판정했고,
   자체 해설도 "기운의 **방출과 절제**의 균형"이라 말한다. 종왕격에 부족한 오행을 보충하는 처방은
   정통 명리 관점에서 **역효과 방향**이다. 우리는 `yongsin`을 이미 손에 쥐고도 min-count 휴리스틱을 쓰고 있다.

> ⚠️ 단, 용신 채택은 코드 교체만으로 끝나지 않는다. 격국별 용신 운용은 유파 차이가 크고,
> **명리 감수(사람 트랙)가 반드시 필요하다.** 이 항목은 "코드 준비 + 감수 대기"로 잡는다.

### 1-3. 나머지 갭

| 갭 | 근거 |
|---|---|
| 오프닝(8카드)과 세션 내용 중복 | `CONSULT-FLOW-HANDOFF.md` §3-7 미해결. 첫인상이 흐려지고 유료 세션 가치가 희석 |
| 꺾는 문장 18종이 draft | `packages/core/src/content/pivots.ts` — 마무리 = 공유 순간인데 카피가 미확정 |
| 비트 간 일관성 보장 없음 | 진단→근거→시기→처방이 각각 생성. 서로 모순돼도 잡는 장치 없음 |
| 공유 설계 부재 | `/r/{token}` OG 이미지뿐. 세션 마무리(가장 공유하고 싶은 순간)에 공유 장치 없음 |
| "정상성 확인" 미대응 | 비교 축이 없음. 방금 만든 타인 명부가 재료가 될 수 있음 |

---

## 2. 트랙 A — 상담 경험

### A1. 오프닝을 "적중의 순간"으로 재정의 ★최우선

지금 오프닝(8카드 덱)은 세션과 겹치면서 아무 역할도 확실히 못 한다. 역할을 하나로 못박는다.
**오프닝의 유일한 임무 = 유료 세션 이전에 "이 앱 뭔가 아네" 를 만드는 것.**

```
현재:  훅 → 일간 → 오행 → 십신 → 시즌 → 주의 → 처방 → …   (세션과 중복)
제안:  훅(일주) → 명식 공개 → ★과거 검증 → 지금 국면 → 고민 허브
                                 ↑ 신규 · 무료 · 여기가 전환 지점
```

**과거 검증 카드** — `daeun` + `seyun`로 구현 가능(재료 이미 있음).

- 대운 전환기(10년 경계)·세운 충돌 연도를 뽑아 "그 무렵 어땠는지" 묻는다.
- **단정 금지, 질문형.** "2019~2021년 즈음, 자리나 사람이 한 번 크게 바뀌지 않았어요?"
- 사용자가 "맞다"를 스스로 채우게 → 신뢰가 **본인 기억으로** 검증된다.
- ⚠️ 가드레일: 과거 진술은 **L2(관망 언어) 이상 필수**. 건강·사망·법적 사건 단정은 L3 차단 대상.
  "힘들었죠"까지는 되지만 "아프셨죠"는 안 된다.

효과: 신뢰 방아쇠 + 무료→유료 전환율 + 소름 = 전파 트리거. **한 장으로 세 개를 친다.**

### A2. 세션 감정 곡선 명시 + 꺾는 문장 확정

4비트를 정보 단위가 아니라 **감정 단위**로 재정의하고 프롬프트에 목표를 박는다.

| 비트 | 감정 목표 | 실패 신호 |
|---|---|---|
| 진단 | 놀람 — "어떻게 알았지" | 누구에게나 맞는 말 |
| 근거 | 납득 — "그래서 그렇구나" | 용어만 나열 |
| 시기 | 희망 — "버틸 이유" | 막연한 낙관 |
| 처방 | 통제감 — "내일 뭘 하면 되지" | 추상적 조언 |
| 꺾는 문장 | 위로 + **공유 충동** | 밋밋한 마무리 |

- `pivots.ts` 18종 draft → final (사람 트랙, 명리·카피 감수)
- 꺾는 문장은 **스크린샷 1장으로 완결**되게 설계 — 이게 A4의 재료가 된다

### A3. "정상성 확인" 대응 — 비교 축 (신규 JTBD)

방금 만든 타인 명부(`StoredPerson`)를 비교 기능으로 확장.

- 궁합(관계 평가)과 **다른 축**: 두 명식을 나란히 놓고 "무엇이 다른가"
- "엄마는 토가 다섯인데 나는 수가 없다" 같은 **구조적 대비** — 대화 소재가 된다
- 원칙 6 유지: 수신자에게 앱 설치 요구 금지

### A4. 전파 설계 — 공유는 별도 기능이 아니라 마무리 카드 그 자체

- 세션 마무리(緣 + 꺾는 문장)를 **그대로 이미지로** 저장/공유
- OG 한글 폰트 임베딩(`lib/og-render.tsx`, 기존 백로그) 선행 필요
- 공유 대상은 결과가 아니라 **문장**. "내 팔자 한 줄" 포맷
- 원칙 6: 공유 URL은 `/r/{token}` 단일 포맷 유지

### A5. 재방문 리듬

- 데일리(曉)는 앱 출시 의존 — 웹에서 먼저 가능한 것: **절기 전환 시점 재방문 훅**
- `boundary.ts`가 절입 계산을 이미 함 → "입추 지났어요, 국면이 바뀌었습니다"
- 대운 전환 연도는 daeun에 있음 → 인생 단위 재방문 트리거

---

## 3. 트랙 B — 해석 품질·정합성

### B1. 동점 버그 수정 ★즉시 (S)

`units.ts` weakestElement 동점 처리. 단독으로도 지금 배포 가능한 수정.
`tests/`에 동점 케이스 회귀 추가(원칙 3: core/saju 변경 시 전체 통과 필수).

### B2. 처방 근거를 min-count → 격국·용신으로 (L, 감수 필요)

- `advanced.geukguk` / `advanced.yongsin` 채택
- 격국별 처방 전략 테이블을 core에 단일 소스로
- **명리 감수 없이 배포 금지.** 코드 준비 → 감수 → 반영 순서
- 캐시 키에 프롬프트 버전 반영돼 있으므로 자연 무효화됨

### B3. 처방 단일 소스화 — LLM 자유생성 차단 ★즉시 (S~M)

오늘 모순의 표면 원인. **색·방향·숫자는 코어가 정하고 LLM은 서사만.**

- `session_remedy` 프롬프트에 코어 처방값을 **주입**하고, 다른 색/방향 생성 금지 지시
- 생성물 후검증: 코어 값과 불일치하는 색·방위 토큰이 있으면 리트라이 → 실패 시 SAFE_FALLBACK
- 가드레일 파이프라인에 붙이면 전 유닛에 적용됨

### B4. 비트 간 · 세션 간 일관성

- **비트 간**: 진단 텍스트를 후속 비트(근거·시기·처방) 프롬프트에 컨텍스트로 전달 — 현재 전달 여부 확인 후 보강
- **세션 간**: 같은 토큰의 다른 주제에서 상반된 진술 방지. chart 요약을 고정 컨텍스트로 사용
- 헤비 유저는 여러 주제를 본다 → **모순은 반드시 들킨다.** 이게 신뢰 붕괴의 1순위 경로

### B5. 재현성

같은 입력 = 같은 출력이어야 한다. "어제랑 다른 말 하네"는 치명적.
캐시 키(`유닛:값:프롬프트버전:시즌:톤모드:관심사`)가 이미 설계돼 있으므로 **실제 적중률 계측**이 과제.

### B6. 품질 계측 (측정 없이는 개선도 없다)

| 지표 | 왜 |
|---|---|
| 캐시 적중률 | 비용 + 재현성 동시 지표 |
| 가드레일 차단율(L1/L2/L3) | 필터가 실제로 일하는지 |
| 폴백 발생률 | SAFE_FALLBACK이 자주 뜨면 프롬프트 실패 |
| 검증가능 진술 비율 | "구체적인가"의 대리 지표 — 헤비 유저 신뢰와 직결 |

---

## 4. 순서 (의존성 기준)

**1차 — 신뢰 누수 먼저 막기** (지금 유료 구간에서 실제로 새고 있음)
1. B1 동점 버그 (S)
2. B3 처방 단일 소스화 (S~M)
3. B4 비트 간 일관성 (M)

**2차 — 신뢰 방아쇠 만들기** (가치 상승폭이 가장 큰 구간)
4. A1 과거 검증 카드 + 오프닝 재정의 (L) ← daeun/seyun 활용
5. A2 감정 곡선 + 꺾는 문장 final (M, 사람 트랙 병행)

**3차 — 전파**
6. A4 마무리 카드 공유 (M, OG 폰트 선행)
7. A3 비교 축 (M)

**병행 — 사람 트랙 / 상시**
- B2 격국·용신 (코드 준비 → **감수** → 반영)
- B6 계측 (1차와 함께 깔면 이후 판단 근거가 됨)
- A5 재방문 리듬 (앱 출시 일정에 종속)

> 1차를 먼저 두는 이유: A1이 아무리 좋아도, 신뢰를 얻은 직후 유료 구간에서 모순된 처방이 나오면
> **얻은 신뢰가 더 크게 무너진다.** 새는 독에 물 붓지 않는 순서.

---

## 5. 결정 사항 — 고객유치·락인 관점 (2026-07-25 확정)

> 판단 기준을 **획득(전파) + 락인(전환비용)** 단일 축으로 고정하고 내린 결정.

### 결정 1 — 과거 검증은 **질문형 + 응답 수집(인터랙티브)**

단정("있었을 겁니다") 대신 **구체적 주장을 질문으로 종결**한다.
> "2019년에서 2021년 사이, 자리나 사람이 한 번 크게 바뀌지 않았어요?"

구체성이 소름을 만들고, 물음표가 리스크를 막는다. 여기에 **[맞아요 / 아니요] 탭을 붙여 응답을 저장한다.**

락인 관점에서 이게 결정적인 이유:
- **자기생성 근거** — 사용자가 자기 기억으로 채운 검증은 우리가 주장한 것보다 훨씬 끈끈하다
- **수동 읽기 → 능동 응답** — 참여 자체가 관여도
- **적중률 캘리브레이션 데이터** — 어떤 대운·세운 휴리스틱이 실제로 맞는지 쌓인다.
  경쟁사가 복제할 수 없는 유일한 자산이고, **쓸수록 정확해지는 제품**이 된다
- **개인 히스토리 = 전환비용** — 답변이 쌓인 앱을 버리고 새로 시작하기 어려워진다

단정형을 버리는 이유: 상방은 비슷한데 하방이 "신뢰 즉사"다. 획득 관점에서도 **틀린 단정은 역방향 전파**(나쁜 입소문)를 만든다.

### 결정 2 — 용신은 **표시만, 결정권은 안 준다** (B2 전면 채택 보류)

- 고객은 용신 유파 차이로 이탈하지 않는다. **같은 화면에 검은색과 초록색이 동시에 뜨는 걸로** 이탈한다.
- 따라서 **보이는 모순**만 먼저 잡는다 → B1 + B3. 명리 감수 불필요, 즉시 배포 가능.
- `advanced.geukguk`("종왕격") 같은 **격국 라벨은 노출**한다. ssaju 산출값이라 우리가 새로 지는 명리 리스크가 없고, 전문성 신호로 획득에 기여한다. **표시하되 판정하지 않는다.**
- 감수자가 확보되면 그때 B2 전면 채택.

### 결정 3 — 오프닝 4장, **`remedy`는 세션으로 이관**

`units.ts:118`의 `remedy` 유닛에 **유료 게이트가 없다.** 무료 8카드에서 색·방향·습관·oneThing을 다 주고,
그 다음 990원에 세션 처방 비트를 판다. **이미 준 걸 다시 파는 구조** — 지금 매출이 새고 있다.

```
오프닝(무료, 4장)   훅(일주) → 명식+격국 → ★과거 검증 → 지금 국면 → 허브
세션(990원/주제)    진단 → 근거 → 시기 → 처방      ← caution·seasonal·remedy 이관
```

무료 구간의 임무는 **"이 앱 뭔가 아네"까지**, 유료 구간의 임무는 **"내 고민에 대한 답"**.
일반론은 공짜로 다 주고(전파 재료), 특정 고민의 깊이는 판다(전환). 헤비 유저는 일반론이 아니라
*자기 상황*에 돈을 낸다.

### 결정 4 — 꺾는 문장은 **draft 배포 후 데이터로 교체**

카피 감수 대기로 막지 않는다. draft 18종을 내보내되 **공유·재방문 지표를 문장별로 계측**해서
실제로 퍼지는 문장을 남긴다. 마무리 카드가 곧 공유 단위이므로 측정 단위가 자연히 일치한다.

---

## 5-1. 결정에 따른 실행 순서 (개정)

| 순서 | 항목 | 감수 필요 | 상태 |
|---|---|---|---|
| 1 | **B1 동점 버그** | 불필요 | ✅ **완료** |
| 2 | **B3 처방 단일 소스화** (LLM 색·방향 생성 차단) | 불필요 | ✅ **완료** |
| 3 | **결정 3 — 처방 카드 무료/유료 분할** | 불필요 | ✅ **완료** (통째 이관 아님 — `PROCESS-DESIGN.md` §4-1) |
| 4 | **A1 과거 검증 카드 + 응답 저장** | 불필요(질문형) | ✅ **완료** (`retro.ts`·RetroCard) |
| 5 | **B4 비트 간 일관성** | 불필요 | ✅ **완료** (진단→후속 비트 컨텍스트) |
| 6 | A4 공유(마무리 카드) + B6 계측 | 불필요 | 다음 |
| 7 | A5 절기 전환 재방문 훅 (웹만으로 가능) | 불필요 | |
| — | B2 용신 전면 채택 | **필요** | 보류 |

> 화면 순서로 옮긴 설계는 **`PROCESS-DESIGN.md`** 참조.

---

## 6. 이번 세션에서 이미 한 것 (미커밋)

**UX**
- 🐛 세션→택일 이동 시 `?token=` 누락으로 **생년월일 재입력을 강요하던 버그** 수정 (`SessionView.tsx`)
- 마무리 카드 선택지 확장 — 유료 사용자가 오히려 선택지가 적던 문제 (웹·앱)
- 타인 사주 = 단독 리포트 (`/p/new`, `app/person/new.tsx`, 기기 로컬 명부)

**B1 — 처방 오행 동점 버그 수정** (`packages/core/src/interpret/units.ts`)
- 최소값 동점 시 `목→화→토→금→수` 순으로 앞선 원소가 무조건 이기던 문제
- 동점일 때만 `advanced.yongsin`으로 가름 — **판단을 새로 만들지 않고 라이브러리 판정을 좁게 빌린다**
  (전면 채택이 아니므로 결정 2와 충돌 없음)
- 실측 검증: 1958-07-02 케이스 `{목0 화1 토5 금2 수0}` → 수정 전 `wood`(초록·동쪽) / 수정 후 `water`(검은색·북쪽)
- 회귀 테스트 3종 추가: 동점 해소 · 비동점 유지 · **결정론(재현성)**
- 이 버그는 무료 덱의 `caution` 유닛(`value`에 weakestElement 포함)에도 전파되고 있었다 → 함께 해소

**B3 — 처방 색·방위 단일 소스화** (`persona.ts` · `interpret.ts` · `remedies.ts`)
- `session_remedy` 지시문이 "색 또는 방향 하나를"이라고만 해서 LLM이 자유 생성 → 코어 처방과 어긋남
- 프롬프트에 코어 처방값 **주입** + "다른 색·방위 지어내지 마세요"
- 생성물 **후검증**(`remedyConflicts`) — 다른 오행의 색·방위가 섞이면 재생성 → 실패 시 폴백
- 한글 단어 경계 부재로 `"검은색"`의 `"은색"`이 금(metal)으로 잡히던 오탐까지 처리(lookbehind)

**결정 3 — 처방 카드 무료/유료 분할** (`ReportDeck` 웹·앱)
- 이 카드는 동시에 **공유 자산**(카카오톡·9:16 스토리)이라 통째 이관하면 전파 경로가 사라진다 → 쪼갬
- 색·방향 = 무료 유지(스샷 자산) / **올해의 한 가지** = 유료 (세션 처방과 직접 경쟁하던 항목)

**실측 검증** (1958-07-02 케이스, 브라우저 + API)
```
무료 덱   당신의 색 검정·남색 / 북쪽    ← 수정 전 초록·청록/동쪽 (B1)
          올해의 한 가지 → "고민 하나를 고르면…"  (결정 3)
세션 처방 "검정·남색 소품을 지니고 북쪽 방향으로…"  ← 코어와 일치, 폴백 없음 (B3)
```

**검증**: typecheck 7/7 · **81 tests**(70→81) · web build · 백엔드 시나리오 · 브라우저 실주행
