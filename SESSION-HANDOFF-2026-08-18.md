# 세션 핸드오프 — 인프라 복구 · 결제 결함 · 무료 베타 전환 · 꿈 해석 (2026-08-18)

> 직전 세션(`SESSION-HANDOFF-2026-08-08.md`)이 남긴 미커밋 7건과 "결정 대기 4건"을 받아,
> 인프라를 복구하고 결제·품질 결함을 정리한 뒤 **판매를 끄고 무료 베타로 전환**한 기록.
> 관련: `DREAM-DESIGN.md`(꿈 설계) · `USABILITY-TEST-2026-08-08.md`(발견 원문) · `CLAUDE.md`(불변원칙).

## 0. 한눈에

- **19커밋, 54파일, +2,945/−80.** 전부 `main`에 푸시·프로덕션 배포 완료.
- 검증: 타입체크 7패키지 · **테스트 110 → 155** · 프로덕션 실브라우저/실API 확인.
- 가장 큰 방향 전환: **운영자가 사업자가 아니므로 판매를 껐다.** 전 구간 무료 개방.

### 이번 세션의 세 가지 전환점

1. **Supabase가 실제로 삭제돼 있었다** — 직전 세션은 "정지 추정"으로 남겼는데, DNS NXDOMAIN이었고
   프로덕션도 같은 죽은 URL을 보고 있었다. 신규 프로젝트로 재구축.
2. **프로덕션 배포가 3주간 실패 상태였다** — turbo 캐시 설정 탓. 아무도 몰랐다.
3. **사업자 미등록** — 결제를 열 수 없다는 사실이 뒤늦게 드러나, 로드맵을 무료 중심으로 재편.

---

## 1. 인프라 복구

### Supabase 재구축
- 구 프로젝트 `chvxifxlgniyfozyfmbd` — ISP·8.8.8.8 양쪽 **NXDOMAIN**. 정지가 아니라 삭제.
- **프로덕션도 같은 URL을 사용 중이었다**(`vercel env`로 확인). `SUPABASE_URL`이 비어 있지 않으면
  인메모리 폴백이 아니라 supabase 어댑터가 선택되고, `putInput`은 에러를 검사하지 않아 **조용히 실패**한다.
  즉 프로덕션은 "캐시 미스" 수준이 아니라 **리포트 저장 자체가 안 되는 상태**였다(사용자에겐 "만료된 링크").
- 신규 프로젝트 `nnysioihnyvcevgvayhg` 생성 → `supabase/schema.sql` 적용(테이블 10종·RLS) →
  Vercel 환경변수 교체(Production/Development/Preview) → 재배포 → 실동작 확인.
- **이전 데이터는 복구 불가.** 베타 사용자에게 나간 `/r/{token}` 링크는 전부 깨진 채로 남는다.
- 초대코드 5개 재발급: `2RT24J K2X9JX EM95GH DKGFSG D35NNT`

### 재발 방지 — 유지 핑을 DB 밖으로 (`9028cc2`)
기존 스케줄(pg_cron)은 **DB 안에** 살아서 DB가 멈추면 같이 멈춘다. 그래서 유지 핑만은
Vercel cron(1일 1회, Hobby 상한)에서 `/api/cron/keepalive`로 돈다. 이 라우트는 헬스체크도 겸해
저장소 왕복 실패를 **503으로 드러낸다** — 쓰기 경로가 에러를 삼켜 소실을 열흘 넘게 몰랐던 게 원인이었다.

> ⚠️ Vercel 대시보드 → Settings → Cron Jobs에서 등록 여부를 눈으로 확인할 것(Hobby는 조용히 무시되는 경우가 있다).

### 배포 파이프라인 (`0061cfc`)
`turbo.json`의 build `outputs`에 `.next/**`가 없어, **캐시 적중 빌드가 산출물을 복원하지 않아**
Vercel이 `routes-manifest.json`을 찾지 못했다. 2026-07-27 프로덕션 배포가 그렇게 실패해 있었고,
그 뒤 3주간 라이브는 그 이전 빌드였다. `outputs`에 `.next/**` 추가(cache 제외) + 환경변수 5종 `env` 선언.

---

## 2. 직전 세션 미커밋 7건 (F1~F7)

전부 커밋·배포 완료. 내용은 `SESSION-HANDOFF-2026-08-08.md` §2 참조.
F5(궁합 결제 링크의 소유자 토큰 누수)는 보안 건이라 특히 확인 요망 — 서버가 `getCompat`으로 역조회한다.

---

## 3. 결제·품질 결함

| | 문제 | 커밋 |
|---|---|---|
| **원칙 9 위반** | 990원 해금에 동의 체크가 없고, 클라이언트가 조작과 무관하게 `withdrawalConsent:true` 고정 전송 — **동의 자체가 성립하지 않았다** | `d92cfac` |
| 원시 에러 | `/s/{token}/health` → `concernById`가 undefined → `guardrailLevel` 접근에서 500. core에 `isConcernId` 타입가드 신설 | `2c5ff8c` |
| 가격 비교 부재 | 990원 구매자 2명 모두의 구매 이유였던 문구가 세션 페이월에만 있었다 → 결제 화면에 세 가격 비교 블록 | `7b34b1a` |
| 기간 고지 0글자 | 7일 패스의 기간·무갱신 안내가 구매 전후 어디에도 없었다 → `Sku.notice` 신설 | `7b34b1a` |
| 상품명 불일치 | `full_report` 라벨이 "풀 리포트 + 문답 1회"인데 실제로는 모든 상담+문답을 연다(횟수 제한 코드 없음) | `7b34b1a` |
| 문장 잘림 | `max_tokens: 320` — 한국어는 토큰당 글자수가 짧아 3문장 지시에서 모자랐다 → 900 + 종결부호 없으면 재생성 | `4020f76` |
| 마크다운 노출 | `stripMarkdown()`으로 **검증 전에** 제거. 반려로 두면 멀쩡한 문장이 재생성 비용만 쓰고 버려진다 | `4020f76` |
| 깨진 음절 | `쯔음`·`믄고` 등은 **고치지 않고 반려.** '믄고'가 믿고인지 묻고인지 코드가 알 수 없다 | `4020f76` |
| 조사 오류 | "부부**으로** 잘 맞는" — 조사 헬퍼가 `season.ts`에 갇혀 있어 궁합이 하드코딩. `core/text/josa.ts`로 분리 | `cb74e17` |

계측에 `rejectQuality`를 추가했다 — 이 반려가 늘면 길이 지시를 손볼 신호다.

---

## 4. 꿈 해석 (무료 리텐션 훅)

설계 전문은 `DREAM-DESIGN.md`. 구현 `ea3c3d7`(사전) + `0897f79`(기능).

**설계를 지배한 제약 하나**: 자유 서술을 받지 않고 **심볼 선택형**으로 간다.
무료인데 조회는 공유를 타고 늘어나므로, 캐시가 무너지면 성공할수록 손해다.
개인화는 **상징 오행 × 원국(부족/과다/그외) 3종**으로 축약해 일간을 키에서 뺐다.

- 캐시 상한 **2,880**(60심볼×감정4×관계3×톤2×모드2)을 테스트로 고정.
- 실측: 프로덕션 **5.3초 → 0.94초**, 키 `dream_reading:teeth_falling|fear|neutral|deep:v4:-:mz:-`.
- 민감 상징 4종(태몽·죽는꿈·고인·돈) **재생성·폴백 0회**로 안전 생성 — 가드레일 그물이 아니라
  프롬프트 단계에서 걸러진다. 해몽 전용 가드레일 3종(태몽 단정·복권 유도·흉몽 프레임) 추가.
- 가드레일이 바뀌면 기존 캐시는 새 규칙으로 재검증되지 않으므로 `PROMPT_VERSION` v3→v4.
  Supabase를 막 새로 만들어 캐시가 비어 있던 시점이라 무효화 비용이 가장 쌌다.

---

## 5. ★ 무료 베타 전환 (`997d99b`)

**운영자가 사업자가 아니다.** 통신판매는 사업자등록·정보표시가 선행돼야 하고, PG 계약 자체가
사업자등록증을 요구한다. 팔 수 없는 것을 파는 UI를 두는 대신 판매를 끄고 전 구간을 열었다.

**스위치는 `lib/commerce.ts` 하나뿐이고, 사업자정보 기입 여부에 묶여 있다.**
플래그를 둘 두면 언젠가 어긋나므로, 실값이 채워지는 순간 자동으로 켜진다.

꺼져 있는 동안:
- `hasFullAccess` 항상 true → 상담 4비트·문답 전부 열림(`lockedBeats: []`)
- 결제 CTA 미렌더(리포트 선물하기·궁합 상세·버티컬 2종)
- `/pay`는 안내 화면, `/api/checkout`·`unlock`은 503
- `/business`는 플레이스홀더 대신 "무료 베타·유료 판매 없음"을 그대로 밝힌다 —
  **없는 사업자를 있는 것처럼 흉내 내지 않는다**
- `/privacy` 삭제요청 조항은 연락처가 없으면 "준비 중" — 없는 주소를 적어
  행사할 수 없는 권리 조항을 만들지 않는다

> ⚠️ **비용**: 전 구간 개방으로 `ctx.paid=true`가 되어 모든 생성이 유료 티어(Sonnet)로 간다.
> 초대코드 5개 규모에선 문제없고 캐시가 받쳐주지만, **베타를 넓히면 티어 분리를 재판단할 것.**

---

## 6. 다음 세션이 받아야 할 것

### 사용자 결정이 필요한 것
1. **개인정보 삭제요청 이메일** — 남은 **유일한 법적 공백**. 사업자가 아니어도 이건 있어야
   조항이 작동한다. 개인 Gmail 노출 vs 서비스용 계정 신설 중 택일 → `BUSINESS_INFO.email`.
2. 직전 세션 결정 대기 중 남은 것: 자녀운 990/9,900원 중복 판매, 앱 푸시 버튼 방향,
   요즘말 모드 톤 강도. (공유 토큰 정책은 무료 전환으로 **당분간 무의미** — 잠글 게 없다.)

### 코드로 가능한 것
- 무료 구간 가치 부족 — 티저 "조심할 것"이 오행 그래프를 문장으로 옮긴 수준
- 뒤로가기 시 상담 진행 전체 리셋(카드 단계가 히스토리에 없음)
- 꿈: 상징 100종 확장, 데일리 푸시 연계(앱 출시 후)

### 명리 감수 필요 (미해결, 백로그 B2)
- **개운 처방이 "없는 오행 채우기" 한 줄 로직** — 신강/신약·용신/기신 구분 없음
- **관계 유형 7종이 라벨 치환** — 동일 상대로 부부/연인 둘 다 같은 점수·같은 문장
- 십신 용어 오용 3건(월지↔월간, 월주↔월간, 극 방향 반대)

### 측정
꿈 기능의 성공 판정은 매출이 아니라 **꿈 사용자의 7일 내 재방문율**이다.
`dream_open`/`dream_symbol_select`/`dream_read`/`dream_to_report`가 붙어 있다.
`dream_read`의 `cacheHitRate`가 수렴하지 않으면 설계 전제가 틀린 것이므로 즉시 중단 판단.

---

## 개발 환경 메모

- 로컬은 `.env.local`에 실제 Supabase 값이 들어 있어 그냥 `pnpm dev`로 뜬다(인메모리 우회 불필요).
- 키·URL 실값은 `SETUP-KEYS.md`에만 있다(`.gitignore:14`, 커밋 이력 없음).
- 재시작은 **포트 지정 kill만** — MCP 서버 보호.
- `packages/api-client`·`core` 변경 시 web 타입체크가 워크스페이스 참조로 바로 잡힌다.
